# Implemented Roadmap Items

Historical completed items moved from `ROADMAP.md` to keep the active roadmap concise.

## 2026-Q2 (May)

- `JUN05-BETA-HONESTY-DOCS` reconciled public planning and docs copy against the current shipped owners on 2026-06-05. The Stage 2 docs-truth regression in `internal/docs/stage2_beta_honesty_docs_test.go` pins the owner split: Go, Python, Dart, Kotlin, Swift, and JavaScript list helpers expose the shipped search parameters; JavaScript and React expose first-factor passkey helpers; `internal/auth/webauthn_mfa.go` still lacks resident-key registration options; file-size guardrails are allowlist-based; and the function-size allowlist is empty. Public wording now uses `ROADMAP.md` for current open gaps, `PRIORITIES.md` for the live guardrail posture, `CHANGELOG.md` for release-facing summary, and `docs-site/guide/beta-limitations.md` as the canonical caveats page linked from the README and docs-site sidebar.

- `JUN04-SEARCH-SYNONYMS` closed the operator-defined search synonyms lane through Stage 4 on 2026-06-05. Backend storage and admin ownership remain in `_ayb_search_synonyms` plus `GET/PUT /api/collections/{table}/synonyms`, while public consumption is proven through the existing JavaScript SDK `records.list(..., { search })` path against a live AYB server. Stage 4 added docs-truth coverage for the shipped contract, updated the canonical search and Algolia migration docs to describe per-collection search synonym groups, and recorded that any future typed SDK/admin synonym-management method remains deferred rather than shipped in `client.records`.

- `JUN04-JS-SDK-NPM-PUBLISH` closed the JS SDK publication lane on 2026-06-04 by publishing `@allyourbase/js@0.2.0` to the live npm registry with `dist-tags.latest=0.2.0`, then terminating verification at the registry and a clean external install rather than at the `npm publish` exit code. Release gates passed on the published artifact before the irreversible step: `sdk/src/types.ts` still declares `highlight?: boolean`, the false-positive guard `! grep -q 'highlight=title' sdk/src/client.test.ts` stayed green, and the full `sdk/` gate (`npm ci`, `npm run typecheck`, `npm test`, `npx eslint src/`, `npm run build`, `npm pack --dry-run`) passed with emitted `dist/index.d.ts` and `dist/index.d.cts` containing `SearchHit`, `FacetCounts`, `typoThreshold?:`, `_highlight`, and the corrected boolean `highlight` contract. Live publish proof on 2026-06-04: `npm whoami --userconfig <tmp>` returned `stuartcrobinsonnpm`; the registry metadata probe returned package `@allyourbase/js` version `0.2.0` with tarball `https://registry.npmjs.org/@allyourbase/js/-/js-0.2.0.tgz`, shasum `f0b4aa18d7aa02c56be0db185e836e6ea493c231`, and integrity `sha512-aJCs4Dfrd915heM7pfA/WYYmpnkK6IY1DlXnyViW0jLioUmUZRBg+4pGnC+1p/68/kGQG//josBU9+YNP0fKHw==`; a temp-project install of `@allyourbase/js@0.2.0` proved the shipped CJS and ESM entrypoints plus the published type declarations. In-repo demos intentionally remain on `file:../../sdk` as the source-of-truth development seam rather than pinning to the registry artifact.

- `JUN03-SEARCH-TYPO-HIGHLIGHT` closed the `typo_threshold` + `_highlight` non-vector search lane on 2026-06-03. Backend owners shipped at `internal/api/handler_list_parse.go` (`parseTypoThresholdParam`, `parseHighlightParam`, plus `unsupportedVectorSearchParams`), `internal/api/search.go` (`defaultTypoThreshold=0.2`, `searchHighlightSQLAlias`, `searchHighlightResponseField=_highlight`, fuzzy threshold wired through `set_limit`, `ts_headline`-based highlight select), `internal/api/handler_list.go` (`scanListItems` + `renameListHighlightAlias` rename the internal SQL alias to `_highlight` in the response), and `internal/api/handler_aggregate.go` (rejects `typo_threshold` + `highlight` on aggregate paths). Vector paths reject the same params via `unsupportedVectorSearchParams`. Behavior contract: default `typo_threshold=0.2`; a custom `typo_threshold` is valid only with `fuzzy=true` and must be in `[0,1]`; `_highlight` opt-in via `highlight=true` and the response field collides cleanly (4xx) with any user column named `_highlight`; aggregate and vector paths reject both params. Proof tests: `TestSearchTypoThreshold`, `TestSearchHighlight`, `TestSearchHighlightRejectsResponseFieldCollision` in `internal/api/integration_test.go`, plus param-rejection coverage in `internal/api/search_test.go`, `internal/api/semantic_query_test.go`, and `internal/api/vector_query_test.go`. JS SDK owners shipped at `sdk/src/types.ts` (`ListParams.typoThreshold`/`highlight`, optional `SearchHit._highlight`), `sdk/src/records.ts` (serializes `typoThreshold` → `typo_threshold` plus the corrected boolean `highlight` query-param surface), with assertions in `sdk/src/client.test.ts` and the public-surface re-export check in `sdk/src/contract.test.ts`. Operator-defined synonyms and non-JS SDK parity for these params remain open in `ROADMAP.md`.
- `JUN03-DEMOS-SDK-LIVE-INTEGRATION` closed the demo/SDK credibility lane on 2026-06-03. The demo release gate is now unambiguous: `Makefile` renamed the narrower cross-demo smoke target from `test-demo-e2e-all` to `test-demo-cross-smoke`, updated the `test-everything` chain to use the new truthful name, and reconciled the corresponding references in `_dev/FEATURES.md` plus the June 3 live-state evidence docs. The live AYB harness owner `scripts/run-with-ayb.sh` now raises `AYB_AUTH_RATE_LIMIT_AUTH` by default so unattended SDK reruns do not trip the sensitive-auth 10/minute limiter, and it enables local storage by default so the live SDK storage coverage runs against the same real server surface. The JS SDK live integration owner `sdk/src/integration.test.ts` now proves three previously unverified contracts against a real AYB server: fuzzy search (`fuzzy: true`) with a typo query gated by a real `pg_trgm` probe, balanced facet counts for `facets: ["category"]`, and WebAuthn MFA HTTP contracts for enroll/challenge/verify error handling; the storage admin path assertions were also reconciled to the current `/api/storage/buckets/` route owner. Green validation commands on this branch tip: `AYB_AUTH_ENABLED=true bash scripts/run-with-ayb.sh "cd sdk && npm run test:integration"`, `cd sdk && npm test`, `cd sdk && npm run typecheck`, `make test-demo-cross-smoke`, `make test-demo-e2e`, and `make test-all`.

- `JUN02-V009-CUT` closes the release-cut proof for `v0.0.9-beta` on the public no-overlay cold-install path. Durable evidence is anchored at `docs/live-state/20260603T095244Z_v009_beta_release_proof.md`, which ties the release publish surfaces (release workflow run `26872377023` on `headSha cbf54938f7cc3b9ffbff8424627a3d7b1dadf9e9` succeeding at 2026-06-03T08:14:50Z; GHCR multi-arch image `ghcr.io/griddlehq/allyourbase:0.0.9-beta` at manifest list digest `sha256:8a2409ec2436df5440cf7fc57a2364fc496c6e83ee3b6acc06a8a0c71a134390` carrying `linux/amd64` + `linux/arm64`) to the GREEN runtime terminators (`run_outcome=success`, `HEALTH_OK: HTTP 200 {"status":"ok","database":"ok"}`, `install=install_sh_remote` with `install.sh` resolving `Latest version: v0.0.9-beta`) and to the `ayb version` subcommand contract `👾 ayb v0.0.9-beta (commit: cbf54938f7cc3b9ffbff8424627a3d7b1dadf9e9, built: 2026-06-03T08:32:45Z)` produced by the released GHCR image. This is the binary that carries the JUN02 wave's user-facing deltas to the public install path: `JUN02-SDK-FACETS-CONTRACT` (facet count queries wired into both list and search paths under RLS, JS SDK contract-tested), `JUN02-DEMO-BROWSER-STANDARDS` (integrated repo-root demo gate green across per-demo lint, local demo harness, and cross-demo Playwright), and `JUN02-SEARCH-FACET-UI-DOCS` (dashboard `Search` view facet column selection + bucket rendering + drill-in plus the dedicated `docs-site/guide/search.md` user guide). The wave closeout artifact is `chats/icg/jun02_pm_0_closeout.md`.

- `JUN02-SEARCH-FACET-UI-DOCS` closed the former-Algolia faceting loop end-to-end on 2026-06-02 by extending the dashboard `Search` view (`ui/src/components/Search.tsx`) with facet-column selection, exact bucket rendering from the shared list response, and bucket-click filter drill-in; adding focused component coverage in `ui/src/components/__tests__/Search.test.tsx`; extending the real-server browser journey in `ui/browser-tests-unmocked/full/search-playground-journey.spec.ts`; and publishing the dedicated user guide `docs-site/guide/search.md` with the REST-reference cross-link in `docs-site/guide/api-reference.md`. Planning docs now reflect the shipped state: the stale facets-open item is removed from `ROADMAP.md`, and `_dev/FEATURES.md` includes the dashboard search/facets surface plus the corrected `search` view inventory counts.

- `JUN02-SDK-FACETS-CONTRACT` wired `parseFacetColumns` + `buildFacetCountQueries` (`internal/api/aggregate.go`) into both offset and cursor list/search paths of `internal/api/handler_list.go`, running the facet count queries on the same `h.withRLS(r)` `Querier` as the page query so counts are RLS-scoped exactly like returned records, and proved the public JS SDK contract end-to-end with two new integration tests in `sdk/src/integration.test.ts` (`records search facets`): a positive test that asserts hand-computed `{a:2, b:1, c:1}` bucket counts alongside a 4-row page, and a negative test that asserts a 400 with `unknown column "nonexistent_col" in facets parameter` from `parseFacetColumns` at `internal/api/aggregate.go:188`.
- `JUN02-DEMO-BROWSER-STANDARDS` closed the six-stage demo browser-test standards lane by proving the integrated repo-root demo gate green across the per-demo lint aliases, the local demo harness, and the cross-demo Playwright suite. The final owner surfaces are `Makefile:317-323` for `test-demo-e2e` / `test-demo-e2e-all`, `_dev/manual_smoke_tests/18_demo_e2e.test.sh` for per-demo orchestration, `tests/e2e/cross_demo.spec.ts` plus `tests/e2e/fixtures.ts` for cross-demo orchestration, and `internal/cli/demo_server.go` / `internal/cli/demo_test.go` for the movies embedded-config runtime contract. Stage 6 caught one integration drift in `tests/e2e/cross_demo.spec.ts`: the password submit selector used a non-exact `Sign In` match that also matched the live-polls passkey CTA; it now uses exact password-submit matching at each local roundtrip sign-in site. Green validation commands: `cd tests/e2e && npm ci --prefer-offline --no-audit && AYB_BIN=/Users/stuart/parallel_development/allyourbase_dev/jun02_pm_2_demos_green_browser_standards/allyourbase_dev/ayb npx playwright install chromium >/dev/null && AYB_BIN=/Users/stuart/parallel_development/allyourbase_dev/jun02_pm_2_demos_green_browser_standards/allyourbase_dev/ayb npx playwright test --reporter=line cross_demo.spec.ts`; `for d in kanban live-polls movies; do (cd examples/$d && npm run lint:browser-tests); done && make test-demo-e2e && make test-demo-e2e-all`.

- `JUN01-SEARCH-PRODUCT-BACKEND-STAGE4` closed the search-product backend lane by locking docs + shipped-history to the Stage 1-3 behavior at HEAD and proving regression gates green. `internal/docs/stage1_app_scoping_docs_test.go` now includes `TestStage4SearchDocsContractsDocumented` to enforce that `docs-site/guide/api-reference.md` owns non-vector list/search semantics (full-text `search`, `fuzzy` opt-in via `pg_trgm`, `facets` response usage, explicit `typo_threshold` rejection, and `400` mode-combination errors) while `docs-site/guide/ai-vector.md` owns vector-mode contracts (supported list paths `nearest`, `semantic_query`, `search+semantic=true`, plus a compatibility matrix documenting non-support for `fuzzy`/`facets`/`typo_threshold` in vector paths and aggregate/hybrid boundaries). This stage also removed overlap/ambiguity between the two guides by keeping vector payload/scoring detail out of the non-vector API reference and ran the required gate set (`gofmt -l`, `golangci-lint`, unit API suite, integration API/server/migrations suite under `testpg`, and `vitepress build`) to green on HEAD.

- `MAY31-COVERAGE-LOAD-TRUTH` closed the five-stage coverage + load-truth + docs-truth maintenance lane on 2026-06-01. Stage 1 was a read-first dependency truth probe: rebased onto `origin/main` and proved the four upstream owners the lane depends on were already shipped — L1 webhook replay handler at `internal/webhooks/handler.go:76` (`r.Post("/{id}/deliveries/{deliveryId}/replay", h.handleReplayDelivery)`), L2 job-runs admin route at `internal/server/routes_admin.go:281` (`r.Get("/{id}/runs", s.handleJobsRuns)`) backed by `internal/server/jobs_handler_jobs.go:84-95` (`handleAdminListJobRuns`) and the shipped migration `internal/migrations/sql/170_ayb_job_runs.sql`, L3 passkeys/WebAuthn backend ownership in `internal/auth/webauthn_mfa.go` with routes registered at `internal/auth/handler.go:160-166` (`/auth/mfa/webauthn/{enroll,enroll/confirm,challenge,verify}`), and L4 passkey dashboard + browser proof at `ui/src/components/Passkeys.tsx` plus `ui/browser-tests-unmocked/full/auth-passkey-lifecycle.spec.ts`. Stage 2 hardened unmocked admin coverage by extending the existing webhook-delivery and job-management specs in-place (no new spec files): replay-button + replayed-status assertions in `ui/browser-tests-unmocked/full/dashboard-webhook-delivery-journey.spec.ts` and a job-run history row-count assertion (both seeded rows) in `ui/browser-tests-unmocked/full/jobs-management.spec.ts` (`ec3a23b1` extend coverage + `c87aff8b` row-count tightening). Stage 3 hardened the movies-demo e2e at `examples/movies/e2e/movies.spec.ts` and the search-result selector seam at `examples/movies/src/components/SearchResults.tsx`, plus the BYOK-clear auth-propagation fix (`5ea6c439` movies: send auth on BYOK clear). Stage 4 reran WS-5000 under the same load harness as the WS-10000 rerun and reclassified it to **graceful backpressure / auth-throttle dominated** (not hard saturation) — artifact `_dev/ws5000_stage1_20260601T103146Z.log` records `5000/5000` iterations completed, `http_req_failed=99.57%`, `4990 × realtime_auth_login failed with status 429`, `realtime_ws_connect` p95 `0s`, and zero hard-saturation markers; the new classification and artifact path are recorded at `_dev/performance_baseline.md:56` and `:73`. Stage 5 (this row) reconciled four stale "Future / Not Yet Built" framings in `_dev/FEATURES.md` against the shipped owners proven above (passkeys, email-template editor UI, webhook replay/dead-letter, job-run history) and appended this closeout entry; open follow-ups (`WEBAUTHN-SDK-CLIENT`, passwordless-first-factor passkey login, `_ayb_job_runs` retention/pruning) remain tracked in `ROADMAP.md:27-29`.
- `MAY31-WS-TIER-DEPTH` closed the Stage 0/1/2 realtime load-tier SSOT refresh on 2026-05-31. `_dev/performance_baseline.md` is now the canonical measurements owner for the refreshed websocket tiers: WS 10000 was rerun in Stage 1 and reclassified as **graceful backpressure / auth-throttle dominated** (`429=9990`, `500=0`, `context canceled=0`, `http_req_failed=99.78%`, `realtime_ws_connect` p95 `0s`, process remained alive through completion), while WS 5000 remains **hard saturation** on carry-forward evidence pending a new dedicated rerun artifact. No correctness-defect fix sprint was entered from this lane because the measured WS 10000 failure mode was throttle/backpressure, not a product correctness collapse. See `_dev/performance_baseline.md` for the full tier table and classification details.
- `MAY31-ARM64-ASSET-REBUILD` closed the published `pg-16` `linux-arm64` asset's mislabeling-by-architecture defect that `MAY24-COLD-MACHINE-INITDB` had identified but only routed around via pgmanager fallback. Lane delivered in five stages: Stage 0 added the red contract test `tests/contract/pg_arm64_asset_arch.sh` that downloads the published archive and asserts `file -b bin/postgres` reports `aarch64`, plus the URL-deriving probe `TestContractProbeDownloadURL` in `internal/pgmanager/platform_contract_probe_test.go`; Stage 1 reworked `.github/workflows/build-postgres-binaries.yml` so the `linux-arm64` job runs on a native arm64 runner and proves the build's architecture at build time before uploading; Stage 2 added a script-level arch guard in `scripts/build-postgres.sh` that refuses to package a binary whose `file -b` output does not match the target platform; Stage 3 synced dev → staging → prod mirrors and republished the `pg-16` tag so the prod-mirror workflow regenerated the asset (prod-mirror run `26720961370` on `headSha b4311a750f7910fe3835b57965935b5d3df401a4`, `displayTitle "sync: promote postgres arm64 build owners from dev main"`, `createdAt 2026-05-31T18:33:22Z`); Stage 4 turned the contract test GREEN against the republished asset and added a guard test `tests/contract/pg_arm64_asset_arch_guard_test.sh` that pins the probe's owner-bounded file-scoped `go test` target so the probe cannot regress to a full-package form that would couple it to unrelated `internal/pgmanager` test health; Stage 5 (this row) recorded closeout evidence and ran regression sanity. Closeout proof: `docs/live-state/20260531T212744Z_pg16_arm64_asset_rebuild_closeout.md` captures the prod-mirror run ID/SHA, the probe's `PG_ARM64_ARCH_OK` line, the derived `CONTRACT_DOWNLOAD_URL=https://github.com/griddlehq/allyourbase/releases/download/pg-16/ayb-postgres-16-linux-arm64.tar.xz`, and the matching `SHA256SUMS` arm64 line (`8dc9e10581fe3c6830f1f56e71fd6f0c91b5621cc315f516d802d38429c76396  ayb-postgres-16-linux-arm64.tar.xz`, independently recomputed against the downloaded archive). The pgmanager runtime fallback path from `MAY24-COLD-MACHINE-INITDB` is preserved — the asset rebuild fixes the source of the defect rather than removing the safety net.
- `MAY31-PARITY-TRUTH` closed the Supabase parity truth-refresh maintenance lane by reconciling parity wording to the refreshed tracker and locking it behind contract checks. Reproducible before/after sources are in `_dev/competitive-research/SUPABASE-PARITY-TRACKER.md` at pre-refresh commit `f460f863` (2026-02-23 snapshot) vs current HEAD (2026-05-31 refresh): overall parity moved from `~53%` to `~93%`, and the summary TOTAL row moved from `132 tracked / 65 shipped / 7 partial / 60 missing` to `136 tracked / 122 shipped / 8 partial / 6 missing`. Row-level status diff from `extract_detailed_rows` (owner: `tests/contract/parity_tracker_citations.sh`) shows `53` stale-row flips across shared rows: `45` `NOT SHIPPED -> SHIPPED`, `5` `PARTIAL -> SHIPPED`, and `3` `NOT SHIPPED -> PARTIAL`. Contract evidence commands: `bash tests/contract/parity_tracker_citations.sh` and `bash tests/contract/parity_tracker_summary_math.sh`.
- `MAY31-WEBAUTHN-MFA-BACKEND` closed the backend lane for WebAuthn (passkeys / FIDO2 security keys) as an MFA step-up method. Schema landed via migration 170: `_ayb_user_mfa` gained `webauthn_credential_id BYTEA`, `webauthn_public_key BYTEA`, `webauthn_sign_count BIGINT`, and `webauthn_aaguid UUID`; `_ayb_mfa_challenges` gained `webauthn_session_data JSONB` for the go-webauthn registration/login session blob. Four routes ship under `/auth/mfa/webauthn/{enroll,enroll/confirm,challenge,verify}` registered through `internal/server/routes_auth.go`, with credential plus ceremony persistence owned by `auth.Service` in `internal/auth/webauthn_mfa.go` and `internal/auth/webauthn_mfa_challenge.go` — RP origin and RP ID are derived from `server.public_base_url` via `newWebAuthnVerifier`. Successful verification mints an AAL2 session token whose AMR claim is `[<first-factor>, webauthn]` via `mfaSessionOptions`; the verify path persists the new `webauthn_sign_count` atomically with `verified_at` and rejects `CloneWarning` (4xx) without mutating the stored counter. The public flag `auth.webauthn_enabled` ships through the single startup-config path `configureAuthCapabilities` and the single runtime path `Handler.SetWebAuthnEnabled`; subsequent `MAY31-WEBAUTHN-UI` work flipped the startup default to ON, and current config validation intentionally leaves `webauthn_enabled` independent from `auth.enabled` because auth-disabled startup does not mount auth services or routes. Published JS/language SDK helpers, first-factor passkey login, and the dashboard UI surface are an explicit deferred follow-up tracked as `WEBAUTHN-SDK-CLIENT` in `ROADMAP.md`.
- `MAY31-WEBAUTHN-UI` closed the dashboard passkeys lane on 2026-06-01. The dashboard now ships owner-bounded passkey enrollment + management in `ui/src/components/Passkeys.tsx`, WebAuthn browser helpers in `ui/src/webauthn.ts`, dashboard API methods in `ui/src/api_passkeys.ts`, and step-up wiring in `ui/src/components/MFAChallenge.tsx` / `MFAEnrollment.tsx`; registered passkeys persist a user-provided display name via migration `171_ayb_webauthn_display_name.sql` and surface it through `GetUserMFAFactors`. The browser proof is `ui/browser-tests-unmocked/full/auth-passkey-lifecycle.spec.ts`: it seeds a distinctive `e2e-passkey-<runId>` name through the real UI, asserts that exact value renders in `data-testid="passkey-name"`, promotes the same session to AAL2 with a CDP virtual authenticator, and then verifies the AAL2-gated backup-code action appears. To keep the proof real, the local browser harness now runs AYB with `--host 0.0.0.0` and a `http://localhost:8090` Playwright origin so the browser origin matches the WebAuthn RP origin AYB derives from `PublicBaseURL()`. With the full UI + CDP proof green, the public startup default is now ON via `auth.webauthn_enabled = true`; runtime toggling still lives at `/api/admin/auth-settings`.
- `MAY31-V008-CUT` closes the release-cut proof for `v0.0.8-beta` on the public no-overlay cold-install path. Durable evidence is anchored at `docs/live-state/20260531T235500Z_v008_beta_release_proof.md`, which ties the Stage 1 RED baseline (`run_outcome=failure`, `FAIL_H0`) to the Stage 3 publish surfaces (release run `26720470396`, Docker run `26720814803`, GHCR `ghcr.io/griddlehq/allyourbase:0.0.8-beta` multi-arch manifest) and the Stage 4 GREEN runtime terminators (`run_outcome=success`, `HEALTH_OK: HTTP 200`). This confirms the `MAY24-COLD-MACHINE-INITDB` pgmanager fallback fix is now in a released binary on the public install path. The release-cut proof and the separate `MAY31-ARM64-ASSET-REBUILD` closeout now combine to cover both paths: the shipped app binary carries the fallback, and the native `pg-16 linux-arm64` managed asset has been rebuilt and re-verified later the same day.
- `MAY31-JOB-RUN-HISTORY` closed the job run history implementation lane with Stage 1-4 shipped surfaces only. Schema contract owner is `internal/migrations/sql/170_ayb_job_runs.sql`, which adds `_ayb_job_runs` plus the per-job started-at index. Atomic per-attempt persistence owner is `internal/jobs/store.go` via `mutateJobAndRecordRun` (single transaction that mutates `_ayb_jobs` and inserts the run row) and read-path owner `(*Store).ListRuns`; execution-path writer is `internal/jobs/service.go` `pollAndProcess`, which records each attempt outcome through the store seam. Admin API owner is `internal/server/jobs_handler_jobs.go` (`handleAdminListJobRuns`, `(*Server).handleJobsRuns`) wired from `internal/server/routes_admin.go` to ship `GET /api/admin/jobs/{id}/runs`. Dashboard owner surfaces are `ui/src/api_admin.ts` (`listJobRuns`) and `ui/src/components/{Jobs.tsx,JobRuns.tsx}` for the runs-view UX. Retention disposition in this closeout is intentionally documentation-only: verified existing retention seams in `internal/jobs/service_store.go` (`(*Service).RegisterDefaultSchedulesWithAuditRetention`), `internal/jobs/handlers.go` (`RegisterBuiltinHandlers`), and `internal/jobs/handlers_builtin.go` (`WebhookDeliveryPruneHandler`, `AuditLogRetentionHandler`, `RequestLogRetentionHandler`) do not include a schedule/handler for `_ayb_edge_function_logs`, whose current owner `internal/edgefunc/log_store_postgres.go` provides persistence/listing only; the `_ayb_job_runs` retention extension remains deferred follow-up in `ROADMAP.md`.

- `MAY24-COLD-MACHINE-INITDB` closed the cold-machine managed-Postgres startup investigation. The lane proved the original `initdb failed: exit status 255` symptom was a stack of separate issues: the published `pg-16` `linux-arm64` asset is mislabeled and actually contains x86_64 `postgres`/`initdb` binaries; the harness had a nounset crash on an empty mount-arg array; after pgmanager started falling back correctly, the harness was still launching AYB as root, which Postgres `initdb` refuses; and the legacy arm64 fallback archive does not ship `pg_cron`, so preloading it made `pg_ctl start` fail. Fixes landed at the existing owners: `internal/pgmanager/extract.go` now validates extracted `postgres --version`, stages installs into a temp dir before promotion, and falls back when the managed-release payload is unusable on the current platform; `internal/pgmanager/fallback_zonky.go` shares the staged-install path to avoid symlink collisions during fallback replacement; `internal/pgmanager/pgmanager.go` drops `pg_cron` from `shared_preload_libraries` on the legacy fallback binary source; and `tools/cold_machine_reproduce.sh` now survives the empty mount-array case, runs the install/start flow as non-root `aybuser`, records `install=head_binary_overlay`, and prints `HEALTH_OK: HTTP 200 {"status":"ok","database":"ok"}` on success. Validation artifact: `docs/live-state/20260530T003540Z_install_repro_postfix.md` with three consecutive green harness runs (`/tmp/coldmachine_stage3_run{1,2,3}_20260530T003540Z.log`). Scope note: those green runs used `HOST_HEAD_BINARY_PATH` to overlay the current repo's `linux/arm64` head binary after the public installer downloaded `v0.0.7-beta`, so the repo state is fixed but a subsequent app release is still required for the no-overlay public install path to inherit the fix.

- `MAY23-LIVE-POLLS-REGISTER` closed with verified multi-stage evidence and no remaining deploy-path drift. Stage 2 landed and validated the GREEN branch fix for post-register auth-state propagation in live-polls. Stage 3 was explicitly non-applicable on the GREEN path (deploy execution remained deferred by contract). Stage 4 passed the live contract gate and captured PASS evidence at `docs/live-state/20260529T234431Z_stage4_live_polls_assertion.log`, `docs/live-state/20260529T234431Z_stage4_live_deployment_sweep.log`, and `docs/live-state/20260529T234431Z_stage4_verification_note.md`. Stage 5 removed the temporary Cloudflare probe workflow and finalized roadmap/workflow closeout at the durable owner path `.github/workflows/deploy_live_polls.yml`.
- `MAY30-MULTIARCH-DOCKER` closed the multi-arch Docker publish gap on `ghcr.io/griddlehq/allyourbase`. Stage 1 added an env-gated contract test (`TestPublishedDockerImageManifestIncludesArm64` in `internal/codehealth/docker_release_runtime_test.go`) that inspects the live manifest via `docker manifest inspect` and fails when `linux/arm64` is missing (default-skips without `AYB_GHCR_MULTIARCH_GATE=1` or when prerequisites are unavailable). Stage 2 extended `.github/workflows/docker.yml` with `docker/setup-qemu-action` plus `docker/setup-buildx-action` and set `platforms: linux/amd64,linux/arm64` on the `docker/build-push-action` step so the build emits a multi-platform OCI image index. Stage 3 executed staging dispatch, prod dispatch (`26670726116`), and the closing prod tag-trigger publish (`26670821710` on tag `v20260530_stage3_multiarch_2`) — all three runs reached `completed/success`. Manifest verification via `docker manifest inspect ghcr.io/griddlehq/allyourbase:latest` confirmed both arches present (`linux/amd64` → `sha256:bc9553db170d…`, `linux/arm64` → `sha256:f5c3049123f9…`, plus two normal OCI attestation entries). Arm64 runtime probe (`docker run --rm --platform linux/arm64 ghcr.io/griddlehq/allyourbase:latest ayb version`) returned `👾 ayb dev (commit: none, built: unknown)`. This is the first time arm64 has appeared in the published manifest, and it removes the QEMU-emulation tax for arm64 hosts pulling the Docker image (the residual `initdb` exit-255 issue tracked under `MAY24-COLD-MACHINE-INITDB` is a managed-Postgres binary concern, not a Docker image arch gap).

- `MAY24-GHCR-REPUBLISH` recorded the Strategy F republish closeout: force-moved `v0.0.7-beta` to prod commit `37d93019`, republished as `as-published-tag=0.0.7-beta` to `ghcr.io/griddlehq/allyourbase` (digest `sha256:514a9736cba0d5cd62d7a6d200b60ba480b07be557372f28fd917d55d8c53a0d`), fixed the Dockerfile issue that had blocked publish, and added the CI `docker-smoke` gate so the publish path is regression-checked.

- `MAY24-SCREEN-SPECS-SCAFFOLD` closed the screen-spec scaffold lane by integrating the implementation ledger at the existing owner (`roadmap/implemented.md`) without creating a parallel log. Delivered outcomes anchored to existing stage artifacts: `docs/screen_specs/DIRMAP.md` now serves as the directory-level index/owner map for screen-spec assets, `docs/screen_specs/_template.md` is the canonical copy-first screen-spec template, `scripts/check_screen_spec.sh` is the single validator seam for required section/order checks, and `docs/screen_specs/_component_login_bar.md` ships the reusable login-bar component spec for downstream screens. Wave 2 dependency context remains explicit for follow-on spec lanes: they should consume these scaffold owners directly rather than re-defining template/validator/component contracts. Scope boundary preserved intentionally: `.debbie.toml` is unchanged, and this Stage 5 closeout is ledger integration only (no new product behavior owners, no reopen of Stage 1-4 implementation owners). (Re-added 2026-05-24 PM after the row was silently dropped when the subsequent `MAY24-GHCR-REPUBLISH` ledger-append commit took the GHCR side on merge.)

- `MAY24-WS-PRESENCE-TEST-ID-CONTRACT` removed two hardcoded `Presences["ws-1"]` assertions from `internal/ws/handler_test.go` that were assuming a fixed connection-ID assignment. `TestHandler_PresenceTrackAndSync` and `TestHandler_ChannelSubscribeSendsPresenceStateForPopulatedChannel` now capture the actual `ClientID` from the `Connected` message returned by each socket and key the presence assertion on the captured value. Added exemplar test `TestHandler_ExemplarPresenceSyncUsesCapturedClientID` (`internal/ws/handler_test.go:922-1006`) that anchors the captured-ID pattern for future authors. Stability verified by Stage 4 stress passes: focused 3-test rerun `-count=100 -parallel 16` green in 0.486s, whole-package `-count=500 -parallel 16` green in 178.855s, race sweep `-race -count=10` green in 4.924s. Sibling sites in `internal/realtime/connection_manager_test.go` and `sdk/src/client.test.ts` audited and dispositioned benign (single-connection harnesses, not racy multi-connection assertions). (Re-added 2026-05-24 PM after the row was silently dropped during Wave 1 merge ordering.)

- `MAY24-COLD-MACHINE-REPRO` recorded the tracked cold-machine install harness outcome from the first sealed-container run on 2026-05-24: `tools/cold_machine_reproduce.sh` against `ubuntu:24.04` reproduced `FAIL_H0` after 106s with `container_exit=4` and the surfaced product failure `Error: starting managed postgres: initializing data directory: initdb failed: exit status 255`. Evidence artifact at `docs/live-state/20260524_175316_utc_install_repro.md`. Contract test at `tests/contract/cold_machine_reproduce_contract.sh` asserts the docker arg ordering that two stage-3 critical bugs broke and fixed in-lane. The remediation follow-up later closed as `MAY24-COLD-MACHINE-INITDB`, with the green proof captured in `docs/live-state/20260530T003540Z_install_repro_postfix.md`.

- `MAY24-INTEGRATION-TEST-RUNTIME-AUDIT` closed the Stage 5 runtime-audit rerun with a variance-aware **NO-WIN** disposition for full-run claimability. The Stage 5 package rerun (`go test -json -tags=integration -count=1 -p 1 ./...`) improved from 254.000s to 215.132s (`-38.868s`) and kept the Stage 3 touched-package reductions visible, while the production entrypoint reruns on 2026-05-24 recorded 531.11s / 521.32s / 529.92s (median 529.92s, spread 9.79s). Because Stage 0 never captured the required three-run production-script baseline cohort, this lane cannot claim a defensible Stage 0→Stage 5 full-run median improvement beyond noise; follow-up ownership remains in `_dev/integration_test_runtime_audit.md` under the existing package/runtime owners and deferred `-p N` rollout path.

- `MAY23-CROSS-DEMO-LIVE-GATE` shipped the `Cross Demo Live` workflow (`.github/workflows/cross_demo_live.yml`) to the public prod mirror (`griddlehq/allyourbase`) with schedule `0 13 * * 1` plus `workflow_dispatch`. First green prod dispatch run: `26348324751` (workflow conclusion `success`; log scan `rg -n '✘|[1-9][0-9]* failed'` returned no hits). Kill-switch if weekly cron is noisy from live infra (not product regressions): `gh workflow disable cross_demo_live.yml --repo griddlehq/allyourbase`.

- `MAY23-TEST-THIN-SEAM-AUDIT` hardened three Tier-A thin-seam tests identified by the `_dev/THIN_SEAM_CANDIDATES.md` audit, adding tight behavior assertions to existing tests in their owner packages: `TestSSEMultipleTables_NoCrossTableDelivery` (`internal/realtime`, proves SSE multi-table subscriptions deliver no cross-table events), `TestAdminDeleteUserSessionSuccess` (`internal/server`, proves the admin session-revoke post-condition), and `TestHandleUpload_SoftTenantQuotaWarning` (`internal/storage`, proves the soft tenant-quota warning path). All three were dispositioned `coverage-only` — no owner production code changed; the seams already behaved correctly and the lane only tightened the assertions guarding them. Stage 4 regression gate at HEAD `85e8715e`: focused reruns green, full touched packages `DONE 2201 tests` green, `gofmt -l` clean, and `make test-all` exit 0 (Go unit+integration 8903 tests, SDK 156, UI 1421).
- `MAY23-OAUTH-RETURN-TO-SDK` closed the SDK-side follow-up deferred at MAY22-OAUTH-RETURN-TO closeout. Added a per-request `redirectTo?: string` option to `OAuthOptions` in the JS SDK (`sdk/src/types.ts`, `sdk/src/auth.ts`), the Dart SDK (`sdk_dart/lib/src/client.dart`'s `signInWithOAuth`), and the React SDK (`sdk_react/src/types.ts` for type parity; `useAuth().signInWithOAuth` forwards the value unchanged because `client.auth.signInWithOAuth` is the actual transport). The SDK appends `&redirect_to=<encoded>` to the OAuth start URL when the option is set, otherwise omits it; no client-side validation, since the server is the single security owner via `validatedOAuthReturnTo` and `AYB_AUTH_OAUTH_RETURN_TO_ALLOWLIST` (see `internal/auth/handler_oauth.go`). TDD red→green: new tests in `sdk/src/auth-oauth.test.ts` cover the redirect_to-present, redirect_to-absent, and redirect_to-with-scopes cases (32 tests pass); symmetric Dart tests in `sdk_dart/test/oauth_flow_test.dart` (215 tests pass); React passthrough test in `sdk_react/tests/useAuth.test.tsx` asserts the option flows to the underlying client (32 tests pass). Docs: `docs-site/guide/authentication.md` Per-request `redirect_to` section now includes JS/Dart SDK helper examples; `docs-site/guide/javascript-sdk.md` and `sdk_dart/README.md` show the option on `signInWithOAuth`. Skipped Python/Swift/Kotlin/Go/SSR — those SDKs have no OAuth helper at all (verified by grep), so adding `redirectTo` to them would first require inventing an OAuth flow for each, which is out of scope of "L2 deferred follow-up."
- `MAY22-CROSS-DEMO-SMOKE` Stage 8 live-deployment Playwright execution: ran the previously deferred live cross-demo suite against the public demo URLs on 2026-05-23 once DNS resolution was confirmed. Command: `cd tests/e2e && CROSS_DEMO_LIVE=1 DEMO_KANBAN_URL=https://kanban.demo.allyourbase.io DEMO_POLLS_URL=https://polls.demo.allyourbase.io DEMO_MOVIES_URL=https://movies.demo.allyourbase.io DEMO_API_URL=https://api.allyourbase.io npx playwright test cross_demo.spec.ts`. Results: 9 passed, 1 failed, 5 skipped (3 intentional `local orchestration` skips with `CROSS_DEMO_LIVE=1`, 1 movies admin-search skip without `DEMO_ADMIN_PASSWORD`, 1 extra). Passes covered anonymous bootstrap default render on all three demos, opt-out auth affordances on all three, kanban authenticated board creation, apex landing index content, OAuth start 307 to github without `redirect_uri_mismatch`, and magic-link request 200. The one failure (`live deployment > live-polls authenticated action creates a poll`) was later closed by `MAY23-LIVE-POLLS-REGISTER`; lane checklist at `chats/icg/may23_am_1_live_polls_post_register_regression.md`. Stage 8 itself is now closed: the test infrastructure landed in L1 Stage 6 is proven runnable against live and its first run surfaced a real bug — the regression net is working as intended.
- `MAY22-4PM-DEMO-DEPLOY` closed the six-stage public demo deployment sprint and promoted it through release mirrors. Delivered scope: deterministic deploy artifacts/runtime config alignment, demo auth-surface extensions, Fly runtime deployment, Cloudflare Pages/DNS routing, live schema + seed data application, and cross-demo live Playwright verification for public endpoints. Stage 6 extended `tests/e2e/fixtures.ts` with `CROSS_DEMO_LIVE=1` env-driven URL orchestration (`DEMO_KANBAN_URL`, `DEMO_POLLS_URL`, `DEMO_MOVIES_URL`, `DEMO_API_URL`) and added a guarded `test.describe(\"live deployment\")` block in `tests/e2e/cross_demo.spec.ts` covering anonymous bootstrap defaults, opt-out auth affordances, one authenticated demo action per app, apex landing content, OAuth start redirect contract, and magic-link request contract. Key commit-range anchor: `6f6ffaf6..HEAD` (resolved on `main` at merge closeout). Validated public URLs: `https://api.allyourbase.io`, `https://demo.allyourbase.io`, `https://kanban.demo.allyourbase.io`, `https://polls.demo.allyourbase.io`, `https://movies.demo.allyourbase.io`. Final live proof run on 2026-05-23 returned HTTP `200` for all five endpoints and `{\"status\":\"ok\",\"database\":\"ok\"}` from `/health`.
- `MAY22-OAUTH-RETURN-TO` documented and released the per-request OAuth `redirect_to` contract. New env: `AYB_AUTH_OAUTH_RETURN_TO_ALLOWLIST` (host-only allowlist entries; no wildcard path matching). `validatedOAuthReturnTo` in `internal/auth/handler_oauth.go` is the single security owner and enforces HTTPS targets except explicit `localhost` / `127.0.0.1` HTTP allowances, with validation at both OAuth start (`handleOAuthRedirect`) and callback dispatch (`dispatchOAuthCallbackResponse`). The selected `returnTo` value is stored server-side in `OAuthStateStore.GenerateWithReturnTo` and consumed one-time at callback via `OAuthStateStore.ValidateAndConsumeReturnTo`, so the callback redirect target is state-keyed with no client-side tamper surface. Empty/missing per-request `redirect_to` keeps `AYB_AUTH_OAUTH_REDIRECT_URL` as fallback destination. Contract proof includes attacker-path regression `internal/auth/oauth_return_to_test.go::TestOAuthRedirect_OpenRedirectAttackerRejected`. Deferred follow-up: extend SDK/client docs/helpers for this per-request redirect capability without widening this server lane.
- `MAY22-CROSS-DEMO-SMOKE` added a top-level Playwright regression net for the demo surface. `tests/e2e/cross_demo.spec.ts` and `tests/e2e/fixtures.ts` now exercise one real roundtrip each for kanban, live-polls, and movies against the built `ayb` binary: kanban persists a new card, live-polls creates a poll and proves the `yes` vote count increments to the exact `1 total vote` / `1 vote (100%)` state, and movies signs in and proves an `inception` search returns `Inception` first. The local flake gate passed 3 consecutive full-suite runs with clean teardown after each pass, `Makefile` gained `test-demo-e2e-all`, and `test-everything` now includes the new cross-demo step. Stage 8 remained parked in this lane because `kanban.demo.allyourbase.io`, `polls.demo.allyourbase.io`, and `movies.demo.allyourbase.io` were not resolvable from the dev environment at closeout time, so the deployed-URL rerun did not execute.
- `MAY22-CROSS-DEMO-SMOKE` Stage 9 closeout: local cross-demo validation is fully landed and release-wired at the existing owners (`Makefile` `test-demo-e2e-all`, chained from `test-everything`; Stage 6 triple-run flake gate passed). Stage 8 remains deferred-until-live based on the 2026-05-22 DNS probe result and is intentionally not reported as executed on deployed hosts. Reuse this exact preflight gate command once live URLs exist: `cd tests/e2e && KANBAN_DEMO_URL=https://kanban.demo.allyourbase.io/ LIVE_POLLS_DEMO_URL=https://polls.demo.allyourbase.io/ MOVIES_DEMO_URL=https://movies.demo.allyourbase.io/ bash -lc 'for url in "$KANBAN_DEMO_URL" "$LIVE_POLLS_DEMO_URL" "$MOVIES_DEMO_URL"; do test -n "$url" || exit 1; curl -fsS "$url" >/dev/null || exit 1; done'`.
- `MAY22-CI-AUTH-LIMIT` restored `AYB_AUTH_RATE_LIMIT_AUTH` at the `test-sdk-integration` job env (`d5295b36`). MAY21-AUTH-LIMITER had removed the script-level override in `scripts/run-with-ayb.sh` so the default `10/min` sensitive-auth limit applies universally to load tests — correct for surfacing WS-1000 / HTTP-1000 auth-bootstrap saturation. The side effect was that the functional SDK integration suite (which makes many rapid register/login calls per test) started 429'ing on the same endpoints. The fix pins the permissive limit only at the integration job env, not back in the script default, so load tests retain the correct 10/min signal while integration tests get the headroom they need.
- `MAY22-SDK-PARITY` auth helper parity shipped across the non-JS SDKs (`a59a77f2`, `f05e4162`, `5e82f68a`, `a0ecc748`, `7e70178a`, `5ec27316`, `bef16b43`, `14c778c5`). Added committed live-contract fixtures plus `tests/contract/sdk_parity_endpoints.sh` coverage for `/api/auth/anonymous`, `/api/auth/magic-link`, `/api/auth/magic-link/confirm`, and `/api/auth/link/email`. React `useAuth()` now exposes `requestMagicLink` and `confirmMagicLink`; SSR adds `confirmMagicLinkServer()` for token-to-cookie landing pages; Dart/Python/Swift/Kotlin/Go add anonymous sign-in, magic-link request/confirm, and email-link wrappers plus `User.is_anonymous` / `linked_at` decoding. Also fixed the Swift test runner package rpath so `swift run AllyourbaseTestRunner` works on Command-Line-Tools-only hosts by loading `lib_TestingInterop.dylib` from `/Library/Developer/CommandLineTools/Library/Developer/usr/lib`. Validation: contract verifier passed both live probe and committed-fixture modes; `pnpm test` + `pnpm run build` for React/SSR, `make test-sdk-dart`, `make test-sdk-python`, `make test-sdk-swift`, `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home PATH="$JAVA_HOME/bin:$PATH" make test-sdk-kotlin`, and `make test-sdk-go` all passed.
- `MAY22-AI-REGISTRY-WIRE` fixed a movies-handler nil-registry bug (`aafe210f`). `wireAIServices` constructed the AI registry and passed it to edge callbacks and the embedding stack, but never called `SetAIRegistry` on the server — leaving movies search, notes embedding, and BYOK chat with a nil registry at runtime once MAY21-DEMOS-D landed. One-line wiring addition; no surface-area change.
- `MAY21-DEMOS-D` (= CL8) brain (movies) corpus demo shipped (`b0f6f9a8`). All movies-demo INFRASTRUCTURE shipped: new `examples/movies/` demo dir; admin-SQL endpoint at `internal/server/sql_handler.go` (POST `/admin/sql` with `{query}` body, `rows[][]any` positional shape); movies handlers in `internal/server/{movies_handler,movies_byok,movies_chat}.go` with vector search (POST `/admin/movies/search`), BYOK key vault lifecycle, and SSE chat streaming over the corpus through `internal/edgefunc/ai_bridge.go` + `internal/ai/registry.go`; new `search_movies` PostgreSQL function + RPC wiring; new `search_movies` MCP tool in `internal/mcp/server_tools.go`; new `movies_reembed` builtin job in `internal/jobs/handlers.go` + `internal/jobs/service_store.go` defaults; demo registered in `internal/cli/demo.go` at port 5177 and embedded via `examples/embed.go`; `chat_messages.metadata JSONB` column added to support partial-persist on client disconnect. The DATA CORPUS deliberately scoped down to a 3-record demo: `examples/movies/seed.sql` contains hand-curated entries for Inception / Arrival / Moonlight with 3-dim toy embeddings (`embeddings.json` is 705 bytes). The full Wikipedia-pull + OpenAI `text-embedding-3-small` 1536-dim corpus pipeline (~10k films, ~60MB binary) is implemented as a separate harness at `examples/movies/rebuild/main.go` (gated by `AYB_MOVIES_REBUILD_EMBEDDINGS=1`); it was NOT executed in this batch and no OpenAI cost was incurred — the harness exists for later expansion if the demo is taken public. TMDB was considered and explicitly rejected for redistribution-license reasons; Wikipedia ToU handling is documented in the rebuild harness for when it runs. Single follow-up landed same day as MAY22-AI-REGISTRY-WIRE above.
- `MAY21-DEMOS-C` (= CL7) live-polls anonymous auth + GraphQL read path shipped (`83593bc1`). Replaced the REST `polls` + `poll_options` two-call bootstrap with a single GraphQL query in `examples/live-polls/src/App.tsx` issued via the new `sdk/src/graphql.ts` `AYBClient.graphql.query()` wrapper (reuses existing client transport + auth-token plumbing; surfaces both transport errors and GraphQL-spec `{errors:[...]}` responses as typed). Votes intentionally stay on REST (GraphQL has no aggregation per `internal/graphql/resolve_select.go:14`). Anonymous visitors now land directly on the poll UI and can vote; poll creation remains explicit-account-only. Idempotent first-visit seed (one poll + ≥2 options + 1 vote via `ayb.records`, sessionStorage-guarded against double-seed) so a stranger always sees a non-empty bar chart on first paint. AuthForm replaced with the shared `<AybLoginBar>` + `useAybAnonymousBootstrap()` from `sdk_react/`. Playwright suite (`examples/live-polls/e2e/{auth,polls}.spec.ts`) updated: anonymous landing assertion, GraphQL request-level proof via `page.waitForRequest`, and explicit sign-out → AuthForm round-trip. Demo runtime precondition: `[graphql] enabled = true` in `ayb.toml` (CL7 adds the config block if absent — verified the section doesn't pre-exist). Note: this lane is the live-polls SOURCE modifications; the Fly DEPLOYMENT (`chats/icg/may19_5pm_4_demo_live_polls_deployment.md`) remains open in `ROADMAP.md` gated on `_dev/OWNER_ACTIONS.md` OA-4/OA-5.
- `MAY21-DEMOS-A` (= CL5) shared demo infrastructure shipped (`4e9d3fde`). Extended `sdk/src/auth.ts` with `signInAnonymously`, `sendMagicLink`, `confirmMagicLink`, `linkEmail` wrappers matching the existing `internal/auth/handler.go` + `handler_magiclink.go` + `anonymous_auth.go` server surface. Widened `sdk/src/types.ts` `User`: `email?: string` (was non-optional) and added `is_anonymous: boolean`; `sdk_react/src/types.ts` `UserLike` widened to match. Added a small client persistence seam in `sdk/src/client.ts` so demos can inject token storage without reading `localStorage` directly. Shipped three new shared React surfaces in `sdk_react/src/`: `AybLoginBar.tsx` (drop-in login UI supporting email-password / magic-link / OAuth via existing `signInWithOAuth`), `useAybAnonymousBootstrap.ts` (provider-owned single-bootstrap hook so concurrent mounts can't double-create anonymous users), and `DemoSuggestionChip.tsx` (demo helper component). Registered `anonymous_user_cleanup` as a builtin job through `internal/jobs/handlers.go` + `handlers_builtin.go` + `service_store.go` defaults (reuses `auth.DefaultAnonymousTTL` rather than duplicating the constant) and wired it in `internal/cli/start_services_wire.go`. Demo packages flipped from npm-registry `@allyourbase/js@^0.1.0` to `file:../../sdk` + `file:../../sdk_react` so the demos consume local SDK builds (`Makefile` extended with `sdk-react-build` phony target that builds both `sdk/dist/` and `sdk_react/dist/` before `make demos` runs `npm ci` per demo dir). New `_dev/FEATURES.md` Demo coverage subsection introduced (dev-only, not synced via debbie). New `_dev/OWNER_ACTIONS.md` OA-6 for the GitHub OAuth app provisioning the demos still need for OAuth-path coverage. Foundation for the three downstream demo lanes (MAY21-DEMOS-B kanban / -C live-polls / -D brain).
- `MAY21-KANBAN-ANON-WS` (= MAY21-DEMOS-B per merge commit subject) kanban anonymous-first + websocket demo lane closed (7 stages). Established the anonymous-first entry boundary in `examples/kanban/src/App.tsx`: anonymous authenticated users land directly in the board shell, `AuthForm` is reachable only as an in-shell upgrade/login entry, and explicit logout opts out of automatic anonymous bootstrap — auth/bootstrap state ownership stays in `useAuth()`, `useAybAnonymousBootstrap()`, and `examples/kanban/src/lib/ayb.ts` with no parallel session logic added. Added idempotent first-user sample-board seeding (`examples/kanban/src/lib/seed.ts`): a fresh user receives exactly one starter board with starter columns and four starter cards. Board realtime sync now rides the SDK websocket subscription transport (`examples/kanban/src/hooks/useRealtime.ts`). The kanban browser specs (`examples/kanban/tests/`) were rewritten to assert anonymous-first, seeding, and websocket behavior, and a live-binary smoke harness was added — `examples/kanban/playwright.smoke.config.ts` (composed from the canonical `playwright.config.ts` defaults) runs selected specs against a live `ayb demo kanban` binary via `npm run test:e2e:smoke`. Stage 7 closeout validation on 2026-05-22 (HEAD `cc59fd95`): SDK build + 143 unit tests + 48 integration tests (incl. full Storage suite, run against an `AYB_STORAGE_ENABLED=true` server) all green; React SDK 21 tests green; kanban `npm ci`/`lint:e2e`/`build`/`test:e2e` green (65 passed, 1 skipped); `make demos` green; binary-backed smoke green (3 specs: anonymous board shell, fresh-user seed, websocket transport). The two Stage 7 validation failures seen in an earlier session were diagnosed as environmental orchestration gaps (integration suite run without a storage-enabled server; smoke run without `ayb demo kanban` serving port 5173), not code defects.
- `MAY21-AUTH-LIMITER` closed the WS 1000 / HTTP 1000 auth-bootstrap load-harness escape. The pre-fix local rerun in this lane reproduced the old failure mode exactly at the auth bootstrap seam: `/tmp/ws1000_repro_server.log` recorded 922 `/api/auth/register` `500`s, 47 `/api/auth/login` `401`s, and 1763 `context canceled` log lines, while k6 never reached a successful websocket upgrade. Root cause was the local harness owner, not server middleware: `scripts/run-with-ayb.sh` had been forcing `AYB_AUTH_RATE_LIMIT_AUTH=10000/min`, masking the default `10/min` sensitive auth limiter on `/api/auth/register` and `/api/auth/login`. Commit `c2b49269` removed that default override while preserving caller-provided values, and `tests/test_load_harness.sh` now asserts both behaviors. Commit `70535af1` added `TestRateLimiterMiddlewareThrottleContract` so the limiter's 429 / `Retry-After` / reset-header contract stays pinned. Verified rerun on 2026-05-21: `make load-realtime-ws-1000-local` still exits red on k6's `http_req_failed` threshold, but that red is now intentional `429`s rather than server collapse — 990 register `429`s, 990 login `429`s, 10 successful register `201`s, 0 register `500`s, 0 `context canceled`, and 10 successful websocket sessions. See `_dev/performance_baseline.md` for the before/after row pair.
- `MAY21-PG-DISPO` recurring `internal/pgmanager` 600s timeout disposition closed on `main`: `cb53f4a9` (`test(pgmanager): fix socket-path overflow and memoize the PG fixture`) is the sole fix that resolved the timeout by shortening Unix socket paths and memoizing the shared PG fixture setup; Stage 2 reruns on `main` recorded two focused `TestManagedPGOutageRecovery` passes at 6.81s and 7.01s test time (about 8s wall), plus package-wide pgmanager integration at 73 pass + 1 expected pgvector skip in 9.333s, versus the pre-fix 122s+ and failing baseline. The unmerged `investigate-pgmanager-600s-timeout` branch (head `105dae03`) is now obsolete evidence: its 5 commits were not needed for the green disposition because the reruns passed on `main` without them.
- `MAY21-AUTH-REFRESH-SKEW` closed the MAR31-PM-3 residual auth/session skew follow-through gap with focused integration proof only, no production code change. Added `internal/auth/sessions_refresh_skew_test.go` to verify `/api/auth/refresh` rejects a refresh token when the authoritative `_ayb_sessions.expires_at` row is already in the past and when it sits exactly at the `NOW()` equality boundary. The equality test also asserts the underlying SQL predicate (`expires_at > NOW()`) excludes the row before the handler maps the miss to the standard 401 + doc URL error envelope.
- `MAY21-SOAK` 75-minute sustained-soak depth proof: extended local soak coverage beyond the prior 12-minute pooled-session rollover proof by running `AYB_SOAK_DURATION=75m K6_VUS=1 make load-sustained-soak-local` against a local AYB target on 2026-05-21. The Stage 2 disposition recorded in `/tmp/soak75m_disposition.txt` was `GREEN exit_code=0 samples=ok:complete=2 malformed_or_orphaned=0 checks_failed=0/1256 leak=pass` — k6 exited cleanly, the merged admin (`/api/admin/stats`) + host (RSS/FD) sampler stream contained no malformed or orphaned timestamps, all 1256 k6 checks passed, and RSS/FD growth stayed within the 1.50× leak threshold across the run. Doc surface: new MAY21 row in the `## Stage 7 Load Harness (k6)` table in `_dev/performance_baseline.md` plus a follow-up line under "Open gaps intentionally deferred beyond Stage 7" that points back to the disposition artifact; P1 realtime/load follow-through clause in `PRIORITIES.md` narrowed to mark the 75-minute depth proof complete. No load semantics, instrumentation, or threshold tooling were changed; only existing owners (`Makefile load-sustained-soak-local`, `tests/load/scenarios/sustained_soak.js`, `internal/server` `/api/admin/stats`) were exercised.
- `MAY20-CI` staging + prod CI fully green for the first time since 2026-03-28. CI had been silently red the whole time; nothing pushed between then and the MAY19 merges, so CL2's new `push` triggers were what made it visible again. Each fix unmasked the next (the `build` job stops at the first failing step). Nine root-cause fixes, no bandaids: (1) `8baf5357` bumped `ci.yml`/`docs.yml`/`release.yml` Node 20→22 because `corepack` pulls pnpm 11, which needs Node ≥22.13. (2) `5c347edf` replaced three broken `pnpm-workspace.yaml` stubs (`ui/`, `sdk_react/`, `sdk_ssr/`) with valid `allowBuilds: {esbuild: true}` — pnpm 11 makes `strictDepBuilds` default and refused esbuild's build script. (3) `cb53f4a9` fixed two pgmanager test-harness bugs: Unix-socket path overflowed macOS's 104-byte `sun_path` limit (fixed with `shortSocketDir()`); and the package overran Go's 10-min per-package timeout because every test rebuilt + re-extracted a byte-identical PG archive (fixed with `sync.Once` + shared read-only bin dir + `TestMain` cleanup). Suite went 122s+/failing → 35s/all-pass. (4) `38b1b7d0` moved `COVERAGE_MATRIX.md` from `_dev/` (never synced to mirrors) to `scripts/` so the gate works on the mirrors. (5) `4b294202` replaced deprecated `reflect.Ptr` with `reflect.Pointer` in 3 files flagged by Go 1.25's golangci-lint govet `inline`. (6) `3377e2fd` is the GHCR/debbie fix from the MAY20 row below. (7) `6d46958c` enabled `AYB_AUTH_ENABLED=true` + a ≥32-char `AYB_AUTH_JWT_SECRET` in the `test-browser` and `test-sdk-integration` job env blocks (they had been starting AYB without auth, so auth-gated routes 404'd — the whole SDK integration suite plus `/api/admin/api-keys` and `/api/admin/users`). Also fixed `make test-smoke` (missing `BROWSER_EXPORT_AUTH_ENV` wrapper) and added a `test-sdk-integration` Make target. (8) `db176b52` extended the `_ayb_ddl_notify` event trigger to fire on `CREATE INDEX` / `ALTER INDEX` (previously fired only on table/view/type/function DDL, so new indexes never appeared in the schema cache — `Table.Indexes` and `GET /api/admin/vector/indexes` both read it); added `index` to `_ayb_drop_notify`; added 2 watcher integration tests. (9) `ea0c3c2a` fixed `handleAdminVectorIndexCreate` racing the eventually-consistent schema cache (spurious `table not found` 400 when a client created a table then immediately indexed it); now forces a synchronous `ReloadWait` on a cache miss; also de-flaked `vector-indexes-lifecycle.spec.ts` (instant `isVisible()` → bounded `waitFor`). Final green runs: staging `ci.yml` `26187270076` (`92f188b`, 13/13 jobs), prod `ci.yml` `26187208703` (`0358b283`, 13/13 jobs). Local verification before pushing: gofmt + golangci-lint + file/function size all clean; `internal/schema` + `internal/server` unit tests 1834 pass; watcher integration tests (5, incl. 2 new) pass; browser smoke 107/0; browser full 60/0; SDK integration 43/43; vector lifecycle 9/9 serial. Session detail: `session_notes/2026-05-20_ghcr_and_ci_repair_handoff.md`.
- `MAY20` GHCR Docker publication resolved — closed the `ghcr-publication` roadmap item, open and deferred as an "owner browser action" since March. Root cause was a debbie sync gap, not an owner action: the post-sync hooks rewrote `owner:` / `REPO=` lines but never the `images: ghcr.io/gridlhq/allyourbase` line in `.github/workflows/docker.yml`, so the prod Docker workflow kept pushing to the old orphan `gridlhq` namespace and 403ing. Added an `images:` namespace rewrite to `.debbie/post-sync-prod.sh` (→ `ghcr.io/griddlehq/allyourbase`) and `.debbie/post-sync-staging.sh` (→ `ghcr.io/gridlhq-staging/allyourbase`), synced dev→staging→prod, and triggered `docker.yml` on prod (run `26178930357` — first success; prior run failed 2026-03-28 with `403`). The `griddlehq/allyourbase` container package is now public, auto-linked to the repo, with anonymous `docker pull` verified. The sync also brought the staging/prod mirrors current with the MAY19-5PM-2/5 merges. See `_dev/RELEASE_SECRETS_AUDIT.md` "Resolution (2026-05-20)".
- `MAY19-5PM-1` org rename propagation (`gridlhq` → `griddlehq`, prod namespace): moved the canonical prod repo identity from `gridlhq/allyourbase` to `griddlehq/allyourbase` after the 2026-05-05 GitHub org transfer. Updated `.debbie.toml` `[repos.prod]`/`[identity.prod]`, install/release surfaces (`install.sh` default `REPO`, `.goreleaser.yaml`, Dockerfile OCI source label), public docs (README, docs-site getting-started/deployment, SDK README), dev-internal docs, packaging manifests (nix/scoop/winget/homebrew-tap), and the debbie post-sync hooks. Updated the Cloudflare `install.allyourbase.io` Worker route to fetch directly from `raw.githubusercontent.com/griddlehq/allyourbase/main/install.sh` (verified live: HTTP 302 → griddlehq). Staging identity (`gridlhq-staging`) deliberately preserved — staging was not moved. 10/10 stages; synced to staging + prod with green CI.
- `MAY19-5PM-2` workflow trigger expansion: extended `.github/workflows/docs.yml` and `test-installer.yml` `on:` blocks from `workflow_dispatch`-only to also fire on `push` to `main` (path-filtered) and `release: published`, so docs/installer regressions surface automatically instead of waiting for a manual dispatch. Confirmed the other four workflows (`ci.yml`, `docker.yml`, `release.yml`, `build-postgres-binaries.yml`) had not drifted from their intended trigger contracts. 5/5 stages; synced staging + prod.
- `MAY19-5PM-5` legacy doc cleanup: rewrote `README_DEV.md` from the defunct `stuartcrobinson/allyourbase` + `sync-to-public.sh` two-repo model to the current debbie dev→staging→prod topology; renamed `chatting/` → `session_notes/` (post-session merge notes); deleted the duplicate `_dev/testing/TESTING.md` in favour of the canonical root `TESTING.md`; added the Firebase "in-tree but not live-validated" honesty caveat to `docs-site/guide/migrations.md`; removed stale `docs-site/.vitepress/.temp/` build-cache artifacts that should never have been tracked; regenerated DIRMAP surfaces. 6/6 stages.

## 2026-Q1 (March / April)

- `MAR31-PM-5` storage fault-confidence closure: added `HandleDelete` error-mapping coverage in `internal/storage/handler_test.go` and expanded `internal/storage/storage_integration_test.go` to cover unauthenticated private serve `401`, owner sign roundtrip success, and cross-user sign `404`. At that time it closed the remaining broad storage handler-confidence gap while leaving auth/session skew as the narrow remaining fault-confidence follow-up; that follow-up was later closed by `MAY21-AUTH-REFRESH-SKEW`.
- `MAR31-PM-4` load capacity closure cascade: audited auth-route throttling ownership (confirmed server-owned in `internal/server/middleware.go` and `routes_auth.go`, no code change needed), wired Argon2id runtime configuration through startup config (`internal/cli/start_services_init.go` → `auth.ConfigureArgon2()`), guarded dangerous local load tiers (HTTP 1000, WS 5000, WS 10000) behind `AYB_LOAD_UNSAFE=1` via Makefile `LOAD_REQUIRE_UNSAFE_LOCAL_TIER` macro, and refreshed `_dev/performance_baseline.md` with per-tier classification summary and cascade outcome documentation.
- `MAR31-PM-3` fault recovery follow-through + Firebase retirement cleanup: added resumable finalize fail-once/recover-next-request proof in `internal/storage/handler_test.go`, introduced a narrow auth-service `now` seam plus JWT skew-boundary validation proof in `internal/auth/{auth.go,auth_jwt.go,auth_test.go}`, and retired Firebase from the remaining secondary docs/specs while preserving the historical in-tree implementation.
- `MAR31-PM-1` staging CI trust hardening: fixed `scripts/run-with-ayb.sh` so existing admin-token files are always backed up/restored correctly and post-run cleanup stays deterministic even when `AYB_ADMIN_PASSWORD` is unset. This closed the staging `build` / `test-integration` blocker without loosening the existing codehealth contract tests; the separately observed `internal/pgmanager` timeout remains triaged follow-up work, not an unresolved runner-script regression.
- `MAR31-AM-3` fault recovery confidence batch: added managed Postgres outage-window recovery proof in `internal/pgmanager/testharness_test.go`, transient storage upload recovery proof in `internal/storage/handler_tenant_quota_test.go`, and deterministic schedule-boundary proof in `internal/server/jobs_handler_{schedules,test}.go`. This closes the long-deferred “managed PG lifecycle test” class of gap and narrows the then-remaining fault-work to broader storage failover + JWT/auth clock-skew behavior; the auth/session skew portion was later closed by `MAY21-AUTH-REFRESH-SKEW`.
- `MAR31-AM-2` load harness hardening + first scale-tier evidence: fixed the shell-quoting/admin-token bootstrap bugs in the k6 harness, added pg-cron-free `-local` wrappers for the HTTP and realtime tier aliases, and guarded a shared-table realtime event-id race in `tests/load/lib/realtime.js`. `_dev/performance_baseline.md` now records real tier outcomes instead of “no server running” deferrals; the 12-minute soak remains open.
- `MAR31-AM-1` staging CI repair and promotion: replaced the racy `select { case start <- ... default: }` websocket test signaling pattern with `sync.Once`-guarded channel close in `internal/server/tenant_quota_middleware_test.go`, validated the full build parity stack locally, then promoted dev `main` to staging commit `e71c73e47b133969fd801c0154244cd3c95ec080` and confirmed GitHub Actions run `23803494948` finished green.
- `MAR30-PM-2` allowlist reduction round 2: decomposed another set of active oversized backend surfaces without changing product behavior. Split `internal/auth/oauth_clients.go` into helper/service files, `internal/jobs/handlers.go` into builtin handler wiring, `internal/jobs/store.go` into schedule-specific persistence, `internal/server/auth_providers_handler.go` into list/probe vs mutation flows, and `internal/ws/handler.go` into dispatch wiring. Shortened `cli.runFunctionsDeploy`, `cli.runStartDetached`, `sbmigrate.Migrator.migrateStorage`, and `sbmigrate.Migrator.migrateSchema` enough to remove all four from the function-size allowlist. Live guardrails now sit at 21 oversized Go files and 22 oversized Go functions.
- `MAR30-PM-1` accessibility closeout and staging-green prep: fixed the remaining 13 staging-surface WCAG failures by correcting pg_cron schema-prefix contrast in the sidebar/header and making the email-template HTML preview scroll region keyboard-focusable. Also cleaned the public roadmap/priorities docs, bumped GitHub Actions to current major versions, and landed posthoc UI fixes for schema-qualified non-public RPC execution, stale email-preview response races, and clipboard error handling in webhooks. The actual `debbie sync staging` and staging-CI verification step remains a separate pending release action on merged `main`.
- `MAR30-AM-3` allowlist reduction and code quality: decomposed 3 oversized files — `cli/demo.go` split into `demo_server.go`/`demo_schema.go`/`demo_serve.go`, `api/import.go` split into `import_parse.go`/`import_sql.go`/`import_validate.go`, `server/server_init.go` split into `server_init_drains.go`/`server_init_replica.go`. Decomposed `api.Handler.execBatchOp` into `batch_exec.go` with per-method helpers. Extracted helpers from 4 oversized functions (`config.applyAuthEnv`, `server.handleEdgeFuncInvoke`, `nhostmigrate.buildPlan`, `schemadiff.FromSchemaCache`). File allowlist reduced by 11 entries, function allowlist reduced by 8 entries. All guardrails pass.
- `MAR30-AM-2` storage quota correctness: fixed TOCTOU vulnerability in TUS resumable upload path — `HandleResumableCreate` now calls atomic `ReserveQuota` instead of read-only `CheckQuota`. Finalization no longer double-counts via `IncrementUsage`. Centralized expired-session cleanup into `storage.CleanupExpiredResumableUploads` (single source of truth for row deletion, temp-file removal, and quota reclamation via `DELETE...RETURNING`). `jobs.ResumableUploadCleanupHandler` delegates to storage-owned cleanup. Integration tests: concurrent oversubscription denial, reservation-at-creation, quota reclamation on expiry, mixed upload paths, overwrite accounting.
- `MAR29-PM-3` accessibility scanning expansion: axe-core WCAG 2.1 AA smoke suite expanded from 7 to 50 tests (48 dashboard views + dashboard home + seeded table browser). Fixed color-contrast WCAG violations across 30+ UI components (text-gray-400→500, text-gray-500→600, text-blue-500→600), fixed sidebar navigation bug (link→button role), added `Organizations.contrast.test.tsx` split for file-size compliance. Build script fix: `dist/.stamp` sentinel preserved across vite builds.
- `MAR29-PM-2` E2E API smoke tests (full journey): `full_journey.test.py` exercising complete user lifecycle (auth → schema setup → webhook listener → CRUD with SSE + webhook assertions → storage upload/download → RPC count → delete with webhook verify → cross-user RLS verification → cleanup). Extracted shared `bash_assert_helpers.sh`, refactored oversized `main()` from 197→64 lines, added `make test-api-journey` target.
- `MAR29-PM-1` SDK parity audit: all 7 non-JS SDKs verified (build + test). Python SDK Ruff import sorting fixed. Swift SDK Sendable warnings removed. Kotlin SDK multipart CRLF escape bug fixed and implicit dependencies added. Per-SDK CI jobs in `.github/workflows/ci.yml` (+141 lines). Per-SDK Makefile targets (`test-sdk-go/python/dart/swift/kotlin/react/ssr/all`). `_dev/FEATURES.md` SDK columns reconciled against actual exports.
- `MAR29-CLEANUP` codebase cleanup checklist: split `tenant_middleware.go` (563 lines) into 5 focused files, decomposed `resolveWhere` (CC 33) into operator helpers, consolidated duplicated `getTestPool`/`execSQL` integration test helpers into `internal/testutil`, removed 1 dead function (`setupSpatial`). Updated file-size allowlist and function-size guardrails.
- `MAR29-PM-10` hardened replica/failover coordination: config validation for duplicate/unparseable replica URLs, health check jitter to prevent thundering herd, operator-facing warnings when all replicas go unhealthy or URL parse fails. 8739 unit tests.
- `MAR28-PM-9` defense-in-depth completion: fixed 6 SMS provider HTTP client timeouts, added SAML cookie Secure flag, added request body size limits to 4 handlers, bounded all server-side external API response reads with LimitReader, comprehensive enterprise audit (zero TODO/FIXME/HACK, zero unsafe.Pointer, zero log.Fatal in library code).
- `MAR28-PM-8` enterprise polish: fixed 4 more HTTP clients missing timeouts, added rows.Err() checks, goroutine panic recovery on all 10 server goroutines, HSTS/Permissions-Policy/X-Permitted-Cross-Domain-Policies headers, explicit HTTP write error discards, math/rand v1→v2 upgrade, Go SDK godoc stubs replaced, 15 new sqlutil/urlutil edge case tests.
- `MAR28-PM-7` review and coverage expansion: integration tests for 4 remaining DB packages (appwritemigrate, directusmigrate, nhostmigrate, fdw), fixed 10 production HTTP clients missing timeouts (billing, storage CDN, Cloudflare, edge func fetch, pgmanager), 100 new unit tests (migrate SQL utils, graphql where-clause, LogBuffer), trimmed PHASES.md from 419→307 lines.
- `MAR28-PM-6` added integration test suites for 3 more packages (ai: history/log/prompt/usage stores, billing: CRUD/upsert/Stripe/webhook/checkpoints, replica: topology CRUD/bootstrap/promote/state). Fixed 3 stale PHASES.md entries (a11y, chaos, fuzz coverage).
- `MAR28-PM-5` added integration test suites for 4 packages (notifications, support, push, extensions), 5 chaos/resilience tests (pool exhaustion recovery, statement timeout, context cancellation, concurrent DDL stress, transaction rollback), fixed 4 stale load contract assertion scripts, restored scope-tracking details in PHASES.md.
- `MAR28-PM-4` expanded unit test coverage round 2 across 7 additional packages (ai, push, tenant, vault, scaffold, httputil, branching, realtime) — ~30 previously untested pure-logic functions.
- `MAR28-PM-3` expanded Go unit test coverage across 7 under-tested packages (status, fdw, schema, support, nhostmigrate, appwritemigrate, directusmigrate) — ~63 new test functions with 100+ test cases.
- `MAR28-PM-2` replaced all 322 `// TODO: Document` placeholders with real GoDoc comments across 90+ files, removed 81 stale auto-generated package comments leaking local paths, removed 2 dead functions, added `internal/sites` unit tests.
- `MAR28-PM-1` completed fuzz hardening (4 new targets: RPC deserialization, import MIME, storage URL rewriting, storage MIME detection), cleaned browser-test lint to zero warnings, added axe-core accessibility scanning (7 WCAG 2.1 AA smoke tests), documented the GHCR publication fix.
- `MAR27-PM-3` refreshed Supabase live-validation, fixed live-fixture seed harness, hardened installer latest-release selection.
- `MAR27-PM-2` restored `make test-all` baseline — integration-only runners, name-collision guardrails, no-cache pgx pool.
- `MAR26-PM-1/2/3` fixed pgmanager test harness, removed uuid-ossp dependency from sbmigrate tests, fixed storage quota concurrent upload timeout.
- `MAR26-BATCH` added pgmanager Zonky fallback, integration test runner, email template refactoring, codehealth guardrails, browser test fixtures, Debbie sync config, CI alignment.
- `MAR24-PM-1/2/3` created audit ledger system, closed auth/security/webhook/trigger proof gaps, promoted 6 audit rows from yellow/red to green.
- `MAR24-PM-4/5/6` fixed scaffold-to-SDK contract, webhook PATCH semantics, verification regressions.
- `MAR23-WS-1/3` corrected RPC notify docs, expanded SDK RPC coverage, completed services DX audit.
- `MAR22-PM-01/02/03` closed docs verification backlog (65 code examples), wired SDK + CLI E2E in CI, hardened PocketBase migration.

### may23_am_1 — Live-polls post-register regression on deployed surface (2026-05-23)
<!-- batman:run_id=a519be67-fcc9-404e-a630-dc1cf426904f -->
> Branch `batman/may23_am_1_live_polls_post_register_regression`

| What | Files | Source |
|------|-------|--------|
| **Live-polls post-register regression on deployed surface** — Applied from roadmap refresh proposal. | `/Users/stuart/parallel_development/allyourbase_dev/may23_am_1_live_polls_post_register_regression/allyourbase_dev/chats/icg/may23_am_1_live_polls_post_register_regression.md` | `roadmap-refresh:a519be67-fcc9-404e-a630-dc1cf426904f` |

### may23_pm_5 — Live cross-demo Playwright CI gate (2026-05-23)
<!-- batman:run_id=eaa44c44-d20b-41cf-96eb-f744bd1b4ac8 -->
> Branch `batman/may23_pm_5_live_cross_demo_ci_gate`

| What | Files | Source |
|------|-------|--------|
| **Live cross-demo Playwright CI gate** — Applied from roadmap refresh proposal. | `/Users/stuart/parallel_development/allyourbase_dev/may23_pm_5_live_cross_demo_ci_gate/allyourbase_dev/chats/icg/may23_pm_5_live_cross_demo_ci_gate.md` | `roadmap-refresh:eaa44c44-d20b-41cf-96eb-f744bd1b4ac8` |

### may23_pm_4 — Cut v0.0.7-beta release (2026-05-24)
<!-- batman:run_id=01da1afc-36e5-415c-bbfb-ad10685a4040 -->
> Branch `batman/may23_pm_4_v007_release_cut`

| What | Files | Source |
|------|-------|--------|
| **Cut v0.0.7-beta release** — Applied from roadmap refresh proposal. | `/Users/stuart/parallel_development/allyourbase_dev/may23_pm_4_v007_release_cut/allyourbase_dev/chats/icg/may23_pm_4_v007_release_cut.md` | `roadmap-refresh:01da1afc-36e5-415c-bbfb-ad10685a4040` |

### may31_pm_4 — MAY31-BROWSER-VERIFY-HARDEN (2026-05-31)
> Branch `batman/may31_pm_4_browser_verified_coverage_depth`

| What | Files | Source |
|------|-------|--------|
| Specs hardened to load-and-verify: 3 (`auth-hooks-lifecycle`, `admin-sql-query`, `first-table-journey`); false-positives retroactively backed by honest evidence: 2 (`auth-hooks-lifecycle`, `admin-sql-query` from `/tmp/l4_audit.md`); FEATURES rows newly flipped to ✅ this stage: 0; deferred remainder: 1 (`movies-search` gap owner pending). | `chats/icg/may31_pm_4_browser_verified_coverage_depth.md` | `stage_02_checklist` |

- `MAY31-WEBHOOK-REPLAY` shipped webhook delivery replay + dead-letter filtering across the existing webhook surface. Backend: `POST /api/webhooks/{id}/deliveries/{deliveryId}/replay` plus `failed_only=true` filtering in `internal/webhooks/{dispatcher,handler,store}.go`; `internal/webhooks/store_integration_test.go` proves the real-Postgres failed-only filter, and `internal/webhooks/dispatcher_test.go` proves replay is single-shot and records a fresh delivery row at `attempt = 1`. Dashboard: `ui/src/{api_webhooks.ts,components/WebhookDeliveryModal.tsx}` now exposes Replay per failed delivery, a Failed only toggle, inline replay result state, and refreshes the delivery list after replay; mocked coverage lives in `ui/src/components/__tests__/Webhooks.test.tsx`.
- `JUN01-JOB-RUNS-RETENTION` shipped `_ayb_job_runs` pruning through the existing built-in jobs retention seam. Runtime owner is `internal/jobs/handlers_builtin.go` `JobRunsRetentionHandler`, which deletes completed run-history rows older than the configured retention window using `finished_at`. Built-in wiring lives in `internal/jobs/handlers.go` (`RegisterBuiltinHandlers`) and `internal/jobs/service_store.go` (`RegisterDefaultSchedulesWithAuditRetention` now seeds `job_runs_retention_daily` with payload `{"retention_days":...}`). Startup threads the config-owned retention from `internal/cli/start_services_wire.go`, and the config owner is `jobs.job_runs_retention_days` with default/env/get/set/validation coverage in `internal/config/*`. Verification: `go run ./internal/testutil/cmd/testpg -- go test -tags=integration ./internal/jobs -count=1`, `go test ./internal/config ./internal/cli ./internal/jobs -count=1`, and `test -z "$(gofmt -l internal/jobs/handlers.go internal/jobs/handlers_builtin.go internal/jobs/handlers_test.go internal/jobs/service_store.go internal/jobs/service_test.go internal/config/config_defaults_sections.go internal/config/config_env_sections.go internal/config/config_getvalue.go internal/config/config_validate_runtime_services.go internal/config/config_validate_sections_test.go internal/config/config_test.go internal/cli/start_services_wire.go internal/cli/start_test.go)"`.
- `JUN01-WEBAUTHN-FIRST-FACTOR-LOGIN` shipped email-first passkey sign-in as a true first factor without removing password fallback. Backend: `internal/auth/handler.go:169-173` mounts `POST /api/auth/webauthn/login/{begin,finish}` and `internal/auth/webauthn_mfa_handlers.go:175-252` reuses the existing WebAuthn owners to start and finish the ceremony for unauthenticated login. Real-Postgres coverage in `internal/auth/webauthn_integration_test.go` proves the contract and hardening with `TestWebAuthnFirstFactorLoginBegin_Contract`, `TestWebAuthnFirstFactorLoginBegin_NullPasswordHashUser_Contract`, `TestWebAuthnFirstFactorLoginBeginFinish_Contract`, `TestWebAuthnFirstFactorLoginFinish_RejectsConsumedChallenge`, and `TestWebAuthnFirstFactorLoginFinish_RejectsExpiredChallenge`; `internal/server/server_test.go` adds `TestAllSensitiveAuthPathsUseStricterLimit` plus `TestWebAuthnFirstFactorAuthRateLimited` so both new endpoints stay under the sensitive-auth limiter. Client surface: `sdk/src/auth.ts`, `sdk_react/src/{useAuth.ts,AybLoginBar.tsx,types.ts}`, and `examples/live-polls/src/components/AuthForm.tsx` now expose a shared "Sign in with a passkey" path. Browser proof lives in `examples/live-polls/e2e/auth-passkey-firstfactor.spec.ts`, which registers a passkey, signs out, signs back in through `/api/auth/webauthn/login/{begin,finish}`, and asserts `/api/auth/login` is never used. Resident-key/discoverable registration did not ship; `internal/auth/webauthn_mfa.go:56-58` still calls `BeginRegistration` without resident-key options, so usernameless login remains deferred.
- `JUN01-SEARCH-PLAYGROUND` shipped the dashboard search playground end-to-end across the existing backend search contract and a new admin UI surface, then closed Stage 4 of `chats/icg/jun01_pm_4_search_dashboard_playground.md` at branch HEAD `d544a548`. Backend ownership is unchanged: query-param parsing remains in `internal/api/handler_list.go:56-166` (`parseSearchParam`, `applyPagination`) and search SQL construction (`pg_trgm` fuzzy + `to_tsvector @@ plainto_tsquery` full-text) lives in `internal/api/search.go:56-99`. Stage 1 added a single new dashboard search-API client owner at `ui/src/api_search.ts:20-83` (`SearchPlaygroundListParams`, `collectionListPath`, `normalizeSearchPlaygroundListResponse`, `listSearchPlaygroundRecords`) — request-path serialization is locked at `ui/src/__tests__/api_search_request_paths.test.ts:12-127` (proves only `search`, `fuzzy`, `filter`, `perPage` are emitted, that `fuzzy` is suppressed when `search` is empty, and that `perPage` is truncated to int) and response normalization at `ui/src/__tests__/api_search_response_shape.test.ts`. Stage 2 added the routed dashboard surface: layout key `"search"` in `ui/src/components/layout-types.ts:8-70`, sidebar entry in `ui/src/components/Sidebar.tsx:83-95`, and renderAdminContent case in `ui/src/components/ContentRouter.tsx:99-203`, with the screen behavior owned by `ui/src/components/Search.tsx:21-260` (table picker, exact/fuzzy/filter inputs, perPage selector, result grid via `TableBrowserGrid`); component coverage lives at `ui/src/components/__tests__/Search.test.tsx`. Stage 3 added the unmocked-against-real-AYB browser proof `ui/browser-tests-unmocked/full/search-playground-journey.spec.ts:11-109` (`Search Playground Journey (Full E2E) › exact, fuzzy toggle, and filter expression return deterministic results`), exercising real `GET /api/collections/<table>?search=&fuzzy=&filter=&perPage=` through the dashboard. Stage 4 regression sweep at HEAD d544a548: `cd ui && pnpm test` → `Test Files 116 passed (116), Tests 1475 passed (1475)`; `cd ui && pnpm build` → `tsc && vite build` exit 0 with `1727 modules transformed` (search surface compiles into the embedded admin UI); targeted journey rerun `bash scripts/run-with-ayb.sh "cd ui && npx playwright test browser-tests-unmocked/full/search-playground-journey.spec.ts --project=full"` → `2 passed (2.7s)`. Pre-existing failures observed and verified-reproduced on unmodified `origin/main` worktree at HEAD `55ba4645`, scoped out of this lane: `make test-smoke` fails `ui/browser-tests-unmocked/smoke/realtime-inspector.spec.ts:88` `opens live realtime activity and returns to baseline after cleanup` identically on both HEADs (`105 passed / 1 failed / 15 skipped`); `make check-sizes` and `make check-func-sizes` both fail on both HEADs against the stale `scripts/allowlist-oversized.txt` baseline last refreshed in commit `2912d02d build: refresh go file-size allowlist baselines` on 2026-05-22 (`internal/auth/oauth.go:586` vs allowlist 575; `internal/api/handler_list.go` missing from allowlist — main 628, this branch 549; `auth.Handler.Routes` 100 lines; `jobs.Service.RegisterDefaultSchedulesWithAuditRetention` 106 lines). This branch's `git diff main..HEAD` modifies zero realtime/ws/inspector files and reduces `handler_list.go` from 628 → 549 lines while not touching the four flagged guardrail owners. Stage 4 closeout details (validation cache rows, REGRESSION-NOTE annotations, and same-HEAD command outcomes) are captured in `validation_cache.jsonl` under session `s164_build_stage4_closeout`.
- `JUN01-RELEASE-READINESS-CLOSEOUT` (umbrella, 2026-06-02) indexes the five Wave-1/2 lanes that landed on `origin/main` at `ec1f942d` ahead of the release-readiness closeout. (1) `jun01_pm_1_search_product_backend` — search backend product surface: full-text via `websearch_to_tsquery('simple', ...)` in `internal/api/search.go`, `pg_trgm` fuzzy via the new migration `internal/migrations/sql/173_ayb_pg_trgm_extension.sql` and the `fuzzy=true/false` query-param path in `internal/api/handler_list.go:57-149`, hybrid full-text + vector merging via reciprocal rank fusion in `internal/api/hybrid_search.go` (`buildFTSHybridQuery`, `buildVectorHybridQuery`, `defaultRRFConstant = 60`), and facet counts in `internal/api/aggregate.go:181-250`; `typo_threshold` remains parked under `unsupportedSearchParams`. Branch `batman/jun01_pm_1_search_product_backend`. (2) `jun01_pm_2_passwordless_passkey_login` — email-first passkey first-factor login. Per-lane closeout is `JUN01-WEBAUTHN-FIRST-FACTOR-LOGIN` above; resident-key / discoverable-credential registration remains parked. Branch `batman/jun01_pm_2_passwordless_passkey_login`. (3) `jun01_pm_3_job_runs_retention` — `_ayb_job_runs` retention. Per-lane closeout is `JUN01-JOB-RUNS-RETENTION` above. Branch `batman/jun01_pm_3_job_runs_retention`. (4) `jun01_pm_4_search_dashboard_playground` — dashboard search playground UI + journey spec. Per-lane closeout is `JUN01-SEARCH-PLAYGROUND` above; facets UI surface remains parked. Branch `batman/jun01_pm_4_search_dashboard_playground`. (5) `jun01_pm_5_js_sdk_search_passkey` — JS SDK search + passkey helpers: `ListParams.search/fuzzy/facets/semantic/semanticQuery/vectorColumn` and `FacetCounts`/`FacetValueCount` in `sdk/src/types.ts`, query-string wiring in `sdk/src/records.ts:40-49`, passkey helpers `beginWebAuthnLogin`/`finishWebAuthnLogin`/`signInWithPasskey` in `sdk/src/auth.ts:90-127`, credential (de)serialization in `sdk/src/webauthn.ts`, and full type re-exports from `sdk/src/index.ts`; other-language SDK parity remains parked. Branch `batman/jun01_pm_5_js_sdk_search_passkey`. Planning-doc reconciliation against `origin/main` at `ec1f942d` ships in this branch (`batman/jun01_pm_6_release_readiness_closeout`) — `_dev/FEATURES.md`, `ROADMAP.md`, and `PRIORITIES.md` are now aligned with what the five lanes actually landed. Probe evidence: `chats/icg/jun01_pm_6_stage1_probe_log.md`.

### L3 — Algolia conversion: "Migrating from Algolia" guide + search-forward onboarding (2026-06-03)
<!-- batman:run_id=f7d3b3d9-0d45-4d22-997a-5a3d7fd15b28 -->
> Branch `batman/jun03_pm_3_algolia_migration_and_search_onboarding`

| What | Files | Source |
|------|-------|--------|
| **Algolia conversion: "Migrating from Algolia" guide + search-forward onboarding** — Applied from roadmap refresh proposal. | `/Users/stuart/parallel_development/allyourbase_dev/jun03_pm_3_algolia_migration_and_search_onboarding/allyourbase_dev/chats/icg/jun03_pm_3_algolia_migration_and_search_onboarding.md` | `roadmap-refresh:f7d3b3d9-0d45-4d22-997a-5a3d7fd15b28` |

### L2 — Search delight: result highlighting (`ts_headline`) + configurable `typo_threshold` (2026-06-03)
<!-- batman:run_id=fa355e32-459e-4ca5-a7a9-64714da1f429 -->
> Branch `batman/jun03_pm_2_search_highlighting_and_typo_threshold`

| What | Files | Source |
|------|-------|--------|
| **Search delight: result highlighting (`ts_headline`) + configurable `typo_threshold`** — Applied from roadmap refresh proposal. | `/Users/stuart/parallel_development/allyourbase_dev/jun03_pm_2_search_highlighting_and_typo_threshold/allyourbase_dev/chats/icg/jun03_pm_2_search_highlighting_and_typo_threshold.md` | `roadmap-refresh:fa355e32-459e-4ca5-a7a9-64714da1f429` |

### L4 (Wave 2) — Publish the updated `@allyourbase/js` SDK to npm (2026-06-03)
<!-- batman:run_id=67ef1455-2822-44b5-9317-f46051aa6e26 -->
> Branch `batman/jun03_pm_4_publish_js_sdk_npm`

| What | Files | Source |
|------|-------|--------|
| **Publish the updated `@allyourbase/js` SDK to npm** — Applied from roadmap refresh proposal. | `/Users/stuart/parallel_development/allyourbase_dev/jun03_pm_4_publish_js_sdk_npm/allyourbase_dev/chats/icg/jun03_pm_4_publish_js_sdk_npm.md` | `roadmap-refresh:67ef1455-2822-44b5-9317-f46051aa6e26` |
