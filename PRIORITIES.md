# Priorities

**Last updated:** 2026-05-31 (post-Wave-1 close-out: cold-machine, live-polls, multi-arch Docker all shipped)

This file is the single source of truth for priority ordering. For feature inventory see `_dev/FEATURES.md`, for execution phases see `_dev/PHASES.md`, for public roadmap see `ROADMAP.md`.

## Summary

No P0 incidents are open. Wave 1 of the `may29_pm` orchestration closed three P1 lanes: `MAY24-COLD-MACHINE-INITDB` (managed-Postgres `initdb` failure — root caused as a mislabeled `pg-16 linux-arm64` release asset that actually shipped x86_64 binaries; fixed in `internal/pgmanager/extract.go` with arch-validation + staged install + fallback path, harness now runs non-root with three consecutive green runs at `docs/live-state/20260530T003540Z_install_repro_postfix.md`); `MAY23-LIVE-POLLS-REGISTER` (Stage 5 closed with live Playwright PASS at `docs/live-state/20260529T234431Z_stage4_live_polls_assertion.log`; durable deploy workflow lives at `.github/workflows/deploy_live_polls.yml`); and `MAY30-MULTIARCH-DOCKER` (`ghcr.io/griddlehq/allyourbase` is now multi-arch — verified via `docker manifest inspect` returning `linux/amd64` + `linux/arm64`). Prod `ci.yml` green; Docker image carries real build-metadata (committed `d84110fb` pre-wave). Near-term focus: realtime/load evidence depth, targeted cleanup in high-churn oversized files, and the still-pending operator org-rename decision (`griddlehq` → `AllyourbaseHQ`).

## P0 — Urgent

- **None currently** — Stage 4 staging sync and CI verification closed on 2026-03-31 for staging commit `e71c73e47b133969fd801c0154244cd3c95ec080` in GitHub Actions run `23803494948` with final conclusion `success`.

## P1 — Important, Next Up

- **Keep CI trustworthy after the MAR31 merge wave** — Recent fixes landed in WS admission tests, load-harness bootstrap/wrappers, schedule timing logic, pgmanager outage recovery proof, storage transient recovery proof, and `scripts/run-with-ayb.sh` admin-token backup/restore handling for staging `build` + `test-integration`. Extend focused proof coverage where the seams are still thin, then keep re-confirming staging after future syncs.
- **Realtime/load follow-through** — HTTP/auth saturation at the safe 100/500 tiers remains classified as capacity pressure, dangerous local tiers (HTTP 1000, WS 1000, WS 5000, WS 10000) remain gated behind `AYB_LOAD_UNSAFE=1`, Argon2 params are runtime-configurable, and `MAY21-AUTH-LIMITER` closed the WS 1000 / HTTP 1000 auth-bootstrap collapse by removing the local `AYB_AUTH_RATE_LIMIT_AUTH=10000/min` override from `scripts/run-with-ayb.sh`. The verified WS 1000 rerun now hits intentional `429` rate limits instead of DB-pool-saturation `500`s. The 75-minute soak depth proof (`AYB_SOAK_DURATION=75m K6_VUS=1 make load-sustained-soak-local`) completed GREEN on 2026-05-21 (`MAY21-SOAK`), closing the "extend soak evidence beyond the 12-minute rollover proof" gap. See `_dev/performance_baseline.md` for the measured tier detail and post-fix rerun evidence.

## Owner Actions (not code work)

Human-only steps — browser clicks, billing decisions, OAuth token grants — are tracked in detail, with status and a dated confirmations log, in `_dev/OWNER_ACTIONS.md`. The GHCR owner actions (OA-1 org package settings, OA-2 `gh` token scopes) were completed by the operator on 2026-05-20 and remain in effect; L1.1's `MAY24-GHCR-REPUBLISH` on 2026-05-24T19:05Z restored pullability for `0.0.7-beta` and `latest` (`v0.0.7-beta` v-prefixed and `dev-c1facd7` remain 404, both as expected). The demo-deployment owner actions OA-4/OA-5 are now completed/obsolete after MAY22-4PM closeout; any future demo OAuth-provider full-flow setup remains separate from this closed deployment lane.

## P2 — Important, Not Urgent

- **Keep code-quality guardrails at zero debt** — The live guardrails are down to 0 oversized files and 0 oversized functions after the 2026-04-08 twenty-first cleanup pass. Keep new work within guardrails by default, and prefer small companion-file splits over growing hot files or orchestration functions back onto a baseline.

## Recently Completed

- **MAY22-4PM demo deployment sprint closeout** — Closed the six-stage deployment lane end-to-end: Stage 1 deterministic Docker/build + runtime config alignment, Stage 2 auth surface extensions for demo flows, Stage 3 Fly deployment of API/runtime, Stage 4 Cloudflare Pages + DNS routing, Stage 5 live schema + seed application, and Stage 6 cross-demo live Playwright verification plus release sync (dev → staging → prod mirrors). Public URLs validated in the closeout evidence set: `https://api.allyourbase.io`, `https://demo.allyourbase.io`, `https://kanban.demo.allyourbase.io`, `https://polls.demo.allyourbase.io`, and `https://movies.demo.allyourbase.io`. Full historical details and commit-range reference are tracked in `roadmap/implemented.md`.
- **MAY21 demos batch (DEMOS-A/B/C/D) + MAY22 follow-ups** — All four MAY21 demo lanes landed on `main`: `MAY21-DEMOS-A` (shared infra in `sdk/`, `sdk_react/`, anonymous-user-cleanup job), `MAY21-DEMOS-B` (kanban anonymous-first + WebSocket realtime), `MAY21-DEMOS-C` (live-polls anonymous landing + GraphQL initial load), and `MAY21-DEMOS-D` (new movies/brain corpus demo with Wikipedia data, OpenAI embeddings, vector search, BYOK chat, admin SQL endpoint, MCP `search_movies` tool). Two same-day follow-ups closed: `MAY22-AI-REGISTRY-WIRE` (the missing `SetAIRegistry` call that left movies handlers with a nil registry) and `MAY22-CI-AUTH-LIMIT` (restored `AYB_AUTH_RATE_LIMIT_AUTH` at the `test-sdk-integration` job env to compensate for MAY21-AUTH-LIMITER making the default `10/min` universal). The demo deployment portion is now fully closed by MAY22-4PM; remaining optional OAuth-provider deep coverage (OA-6) is tracked separately from the released deployment lane. Full detail in `roadmap/implemented.md`.
- **Auth-throttle load-harness fix (MAY21-AUTH-LIMITER)** — `scripts/run-with-ayb.sh` no longer forces `AYB_AUTH_RATE_LIMIT_AUTH=10000/min` during local load runs, so the default `10/min` sensitive auth limiter now protects the shared HTTP 1000 / WS 1000 auth-bootstrap seam. Focused harness + rate-limit tests are green, and the verified WS 1000 rerun replaced the old `500`/`context canceled` collapse with intentional `429`s and 0 register-path `500`s. See `roadmap/implemented.md` and `_dev/performance_baseline.md`.
- **MAY21-PG-DISPO (pgmanager 600s timeout disposition)** — Closed the previously open recurring `internal/pgmanager` timeout lane after Stage 2 reruns passed green on `main`; `cb53f4a9` is the resolving fix (socket-path overflow + memoized PG fixture) and the unmerged `investigate-pgmanager-600s-timeout` branch (`105dae03`) is now obsolete evidence. Full disposition detail: `roadmap/implemented.md` (`MAY21-PG-DISPO`).
- **Auth/session skew follow-through closure (MAY21-AUTH-REFRESH-SKEW)** — Added focused integration coverage in `internal/auth/sessions_refresh_skew_test.go` proving `/api/auth/refresh` rejects sessions whose `_ayb_sessions.expires_at` row is already in the past and at the exact `NOW()` equality boundary. This closes the MAR31-PM-3 residual auth/session skew follow-through gap without changing production behavior.
- **Staging + prod CI fully green (MAY20-CI)** — First green CI on both mirrors since 2026-03-28. Nine root-cause fixes (Node 22, pnpm `allowBuilds`, pgmanager socket-path + memoized fixture, coverage-matrix gate moved to `scripts/`, `reflect.Ptr`→`reflect.Pointer`, debbie GHCR `images:` rewrite, CI auth env for `test-browser` + `test-sdk-integration`, `_ayb_ddl_notify` invalidation on `CREATE INDEX`/`ALTER INDEX`, synchronous schema reload on vector-index validation cache miss). Full detail and verification matrix in `roadmap/implemented.md` (`MAY20-CI`) and `session_notes/2026-05-20_ghcr_and_ci_repair_handoff.md`.
- **GHCR Docker publication resolved (MAY20, then republished MAY24)** — Closed the `ghcr-publication` item, deferred since March. Root cause was a debbie sync gap (post-sync hooks rewrote `owner:`/`REPO=` lines but not the `images:` line in `docker.yml`), fixed in `.debbie/post-sync-prod.sh` + `.debbie/post-sync-staging.sh`. On 2026-05-20, `docker.yml` ran green on prod and anonymous pull proof was captured. The v0.0.7-beta release cut (`MAY23-V007-CUT`) shipped without a republish-on-release path, so the GHCR `0.0.7-beta` manifest was absent for ~5 hours; `MAY24-GHCR-REPUBLISH` fixed the Dockerfile demo-builder rollup error, added a CI `docker-smoke` gate, and republished `0.0.7-beta` + `latest` to pullable state (digest `sha256:514a9736…3a0d`, re-probed 2026-05-24T23:33Z). See `roadmap/implemented.md` (`MAY20`, `MAY24-GHCR-REPUBLISH`) and `_dev/RELEASE_SECRETS_AUDIT.md`.
- **Org rename propagation `gridlhq` → `griddlehq` (MAY19-5PM-1)** — Moved the canonical prod repo identity to `griddlehq/allyourbase` across `.debbie.toml`, install/release surfaces, public + dev-internal docs, packaging manifests, and debbie post-sync hooks; repointed the Cloudflare `install.allyourbase.io` Worker route at `raw.githubusercontent.com/griddlehq/allyourbase` (verified live). Staging identity (`gridlhq-staging`) preserved. 10/10 stages, synced staging + prod.
- **Workflow trigger expansion (MAY19-5PM-2)** — `docs.yml` and `test-installer.yml` now fire on `push` to `main` (path-filtered) and `release: published`, not just `workflow_dispatch`, so docs/installer regressions surface automatically. Other four workflows verified un-drifted. 5/5 stages.
- **Legacy doc cleanup (MAY19-5PM-5)** — Rewrote `README_DEV.md` to the current debbie dev→staging→prod topology, renamed `chatting/` → `session_notes/`, deleted the duplicate `_dev/testing/TESTING.md`, added the Firebase live-validation honesty caveat to `docs-site/guide/migrations.md`, and purged stale `docs-site/.vitepress/.temp/` build-cache artifacts. 6/6 stages.
- **Load capacity closure cascade (MAR31-PM-4)** — Audited auth-route throttling ownership (confirmed server-owned, no code change needed), wired Argon2 runtime configuration through startup config, guarded dangerous local load tiers (HTTP 1000, WS 5000, WS 10000) behind `AYB_LOAD_UNSAFE=1` in Makefile, and refreshed capacity docs with per-tier classification and cascade outcomes in `_dev/performance_baseline.md`.
- **Storage fault-confidence closure (MAR31-PM-5)** — Added `HandleDelete` error-mapping unit coverage in `internal/storage/handler_test.go` and expanded storage RLS integration coverage in `internal/storage/storage_integration_test.go` for unauthenticated serve `401`, owner sign roundtrip success, and cross-user sign `404`.
- **Fault recovery follow-through + Firebase retirement cleanup (MAR31-PM-3)** — Added resumable finalize fail-once/recover-next-request proof in `internal/storage/handler_test.go`, introduced a narrow auth-service clock seam plus JWT skew-boundary proof in `internal/auth/{auth.go,auth_jwt.go,auth_test.go}`, and retired Firebase from the remaining secondary docs/specs (`_dev/COMMANDS.md`, `_dev/IMPLEMENTATION_BRIEF.md`, `_dev/PROJECT_PROPOSAL.md`, `docs/BDD_SPECIFICATIONS.md`, `tests/specs/migration.md`, `_dev/migration_test/LIVE_MIGRATION_TEST_PLAN.md`).
- **Staging CI trust hardening (MAR31-PM-1)** — Fixed `scripts/run-with-ayb.sh` so pre-existing admin-token files are always backed up/restored correctly, validated the existing codehealth coverage for that contract, and closed the deterministic staging blocker that had been making `build` and `test-integration` untrustworthy.
- **Fault recovery confidence batch (MAR31-AM-3)** — Added managed Postgres outage recovery proof in `internal/pgmanager/testharness_test.go`, transient storage upload recovery proof in `internal/storage/handler_tenant_quota_test.go`, and deterministic schedule-boundary proof in `internal/server/jobs_handler_{schedules,test}.go`.
- **Load harness hardening + first scale-tier evidence (MAR31-AM-2)** — Fixed the load bootstrap quoting/token-resolution bugs, added pg-cron-free `-local` wrappers for HTTP/realtime tier entry points, guarded the shared-table realtime event-id race, and replaced the old “no server running” scale-tier deferral with real measured outcomes in `_dev/performance_baseline.md`.
- **Stage 4 staging promotion verification (MAR31-AM-1)** — Promoted dev `main` snapshot to `gridl-staging/allyourbase` as staging commit `e71c73e47b133969fd801c0154244cd3c95ec080`, observed run `23803494948` complete green, and captured staging evidence in `session_notes/2026-03-31_post_merge_doc_audit_and_next_steps.md`.
- **Allowlist reduction round 2 (MAR30-PM-2)** — Split oversized auth, jobs, server, and ws files into focused seams; shortened the remaining Stage 4 target functions in `cli` and `sbmigrate`; reduced the live guardrails to 21 oversized Go files and 22 oversized Go functions. Targeted security fixes also landed for detached-start logging and unsafe function-deploy lookup IDs.
- **A11y/staging-green prep (MAR30-PM-1)** — Fixed the remaining 13 staging-surface accessibility failures (schema-prefix contrast and email-preview keyboard focus), cleaned public roadmap/priorities docs, bumped GitHub Actions to current majors, and landed posthoc UI fixes for schema-qualified non-public RPC execution, stale email preview responses, and clipboard failure handling. The staging promotion follow-through was completed in MAR31-AM-1.
- **Storage quota correctness (MAR30-AM-2)** — Fixed TOCTOU vulnerability in TUS resumable upload path. Atomic `ReserveQuota` replaces read-only `CheckQuota` at session creation. Cleanup reclaims reserved bytes on session expiry. Centralized cleanup into storage-owned helper, delegated scheduled job through it. Integration tests cover concurrent oversubscription, mixed upload paths, and quota leak scenarios.
- **Allowlist reduction and code quality (MAR30-AM-3)** — Decomposed 3 oversized files (cli/demo.go, api/import.go, server/server_init.go) and 4+ oversized functions (config.applyAuthEnv, server.handleEdgeFuncInvoke, nhostmigrate.buildPlan, schemadiff.FromSchemaCache). File allowlist reduced by 11 entries, function allowlist reduced by 8 entries. Dead code removal via deadcode/staticcheck.
- **E2E API smoke tests (full journey)** — `full_journey.test.py` exercises complete user lifecycle (auth → CRUD → realtime SSE → storage → webhook → RPC → cross-user RLS). Wired into `make test-api-journey` and `make test-api-smoke`. Shared bash assertion helpers extracted to `tests/bash_assert_helpers.sh`.
